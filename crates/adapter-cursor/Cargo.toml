[package]
name = "agent-observability-adapter-cursor"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
agent-observability-contracts = { path = "../contracts" }
agent-observability-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11.0"

[dev-dependencies]
agent-observability-local-store = { path = "../local-store" }

[lints]
workspace = true
