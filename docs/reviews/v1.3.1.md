# v1.3.1 Release Review

This record captures the patch release that corrects v1.3.0 publication. Product runtime behavior
is unchanged; the patch fixes universal-binary verification, advances synchronized distribution
versions, and preserves the immutable failed tag as history.

## Final verdict

- **Released:** exact-head local checks, GitHub CI, real-artifact `lipo` verification, and
  role-separated review passed.
- **Reviewed source:** `1916dec4acdc93d56f669a5d27d1c7952158fa7b`.
- **Base:** v1.3.0 merge commit `166448407ddf071493f833deeba083f07f0fb537`.
- **Boundary:** no domain, adapter, storage, runtime, report, privacy, or network behavior changed.

## Correction

Tag workflow run `33412746641` built both v1.3.0 native artifacts, then failed before attestation,
Release creation, or Package publication. The workflow passed the input file after
`lipo -verify_arch`; Xcode `lipo` requires the input file first. v1.3.1 uses:

```bash
lipo stage/agent-observability -verify_arch arm64 x86_64
```

The workflow text is regression-locked. More importantly, the arm64 and x64 artifacts from failed
run `33412746641` were downloaded, combined locally, and checked with that exact corrected command;
`lipo -archs` reported `x86_64 arm64`, and the universal executable reported version `1.3.0`.
The v1.3.1 tag workflow rebuilds the same source path at version `1.3.1` and executes this check
before attestation or publication.

## Required verification

- `cargo fmt --all -- --check`: passed.
- `cargo test --workspace --no-fail-fast`: passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `npm test`: 67 tests passed plus desktop, mobile, and 4,096-span browser smoke with no overflow,
  console errors, or external requests.
- `actionlint .github/workflows/*.yml`: passed.
- Exact-head GitHub `Rust` and `Report UI` checks: passed.
- Cargo, root npm, GitHub Package, lockfiles, README commands, architecture/runtime docs, example
  metadata, and release retry fixtures all use `1.3.1`.
- PR head is mergeable and the worktree is clean.

## Independent review evidence

- `code-reviewer` reviewed `1916dec4acdc93d56f669a5d27d1c7952158fa7b` and returned `PASS`.
  Its only LOW observation was that the workflow-text regression assertion alone does not execute
  `lipo`; the real failed-run artifacts were therefore assembled and verified separately as
  described above, while the tag workflow remains the final executable gate.
- `verifier` reviewed `1916dec4acdc93d56f669a5d27d1c7952158fa7b` with exact-head local and
  GitHub evidence and returned `PASS` with no remaining gap.

## Publication gate

After merge, annotated tag `v1.3.1` must point to the resulting `main` merge commit. Completion
requires a successful tag workflow plus verification of Release assets, checksums, provenance,
universal binary architectures and version, and GitHub Package version. The v1.3.0 tag remains
untouched and has no Release or Package.
