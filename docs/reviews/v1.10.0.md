# v1.10.0 Release Review

Status: **Candidate — not released**. PR #39 remains draft until final-head CI and automatic
release performance evidence are complete. Publication and installed-runtime QA
are separate downstream gates.

## Scope

- Report v2 explains unavailable fields and distinguishes partial token totals from complete totals.
- Verified project context is pseudonymous; exact cwd and source-provided messages are separately
  available only for future eligible Codex notify turns with explicit local opt-in.
- Raw detail never enters canonical SQLite, report DTO/static HTML, archive/export or team transport.
- Private artifact availability requires synchronized detail and terminal status, consistent success
  tuples, bounded storage/retention and startup orphan reconciliation.
- Report v1 and runtime config v2 remain frozen compatibility contracts with explicit migrations.

## Independent findings and resolution

| Review | Reviewed revision | Result and resolution |
| --- | --- | --- |
| Verifier | `3308806` | PASS after fixing valid partial-token v1 migration and stale asynchronous-publication documentation; 17 contract tests passed. |
| Code reviewer | `3531261` | APPROVE, zero findings across 76 PR files and 19 final-commit files. Inconsistent HTTP/state/code tuples were fixed in `942e17b`; independent recheck passed, including private-detail and contract parity tests. |
| Architect | `3531261` | WATCH, non-blocking: closed reason/state and shared private schema resolve the original blockers. Required-nullable serialization resolves the additional 64 KiB mismatch. No product release blocker remains; deferred coupling items are listed below. |

Reviewers are independent native role-separated agents. Author validation is not substituted for
their final approval. Earlier reviewers interrupted by usage limits did not provide approval.
Combined recommendation: **COMMENT** because the architecture watchlist is non-blocking; this is
not an unconditional architecture CLEAR. All identified blocking findings have been resolved.

## Validation evidence

- At `3308806`: full Rust workspace tests, clippy with warnings denied, formatting, and `npm test`
  passed locally. Dispatch run `33977775344` passed the three prerequisite jobs, then was cancelled
  because subsequent fixes superseded its revision. PR run `33977765913` failed macOS automatic
  performance smoke; the later `067b29f` PR run `33978387003` passed all three jobs.
- The partial-token regression failed before the migration fix and passed afterward.
- At `942e17b`: all 15 private-detail tests and targeted collector clippy passed.
- At `067b29f`: shared report parity covers every declared state/reason combination plus raw email,
  raw path, raw-error and contradictory-state rejection. Rust and schema-based TypeScript checks passed.
- At `3531261`: full Rust workspace tests, workspace clippy with warnings denied, formatting and
  full `npm test` passed after shared-schema integration. Web checks include 34 unit tests,
  desktop/mobile report and dashboard tests, a 4,096-span bounded rendering case, and settings
  desktop/mobile/compact tests with no overflow, console errors or external requests.
- At `3531261`: optimized release build reports `1.10.0`; GitHub PR run `33979054719` passed
  Rust, Rust macOS (including automatic smoke) and Report UI jobs.
- One earlier local workspace run failed the 250 ms notify acceptance assertion during concurrent
  compile/browser work. Two subsequent full workspace runs passed; the production deadline was
  not widened. Final remote automatic performance evidence remains required.
- Private-detail parity rejects an absent required nullable assistant message and checks explicit
  null at 65,536 bytes (accepted) and 65,537 bytes (rejected) in Rust and generated TypeScript.

## Chrome computer-use QA

- Real stored observations were inspected through Chrome. A private, isolated SQLite snapshot held
  12,435 observations across 67 sessions; report-v2 detail panels explained missing source fields.
- An independent synthetic fixture showed cwd, request and response in the local detail panel.
  Disabling private capture then denied the same lookup. These are synthetic UI checks, not evidence
  that real Codex raw content was captured.
- Synthetic private strings were absent from generated HTML and canonical store artifacts.
- The installed collector still generated the previous report format during candidate QA, so the
  candidate used the isolated snapshot. Final public-install/live collection QA is still required.
- Supported connection reconciliation restored `connected / ready / external_preserved` after
  unrelated model/app configuration edits. Existing external notify was not replaced.

## Remaining boundaries

- Existing external Codex notify remains preserved; private details are unavailable unless agentobs
  owns and receives an eligible callback. Enabling the local option alone does not create that source.
- No historical raw reconstruction, full API wire capture or automatic Claude Code/Cursor collection.
- Commercial team remains TODO. Before enabling it, reconcile future-team report-v1 references with
  the current report contract and move source-specific lookup policy behind a typed adapter capability.
- A shared 250 ms transport deadline is not a hard-real-time guarantee for blocking filesystem calls.
- Do not mark this version Released until the release, package, checksums/attestations and installed
  runtime checks are verified; publication documentation belongs in a separate reviewed docs PR.
