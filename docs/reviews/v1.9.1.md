# v1.9.1 Release Review

Status: **Release candidate**. This review covers the settings-status and LaunchAgent recovery hotfix at exact
source `f334144b8a3a2230ddb7e31e67bb8d9e95e711a8`. Publication evidence is recorded after merge and tag.

## Scope

- Refresh Codex integration state on focus and active-session heartbeat without reloading the settings page.
- Retry one transient `connected + collector unavailable` startup result, then stop.
- Reject stale overlapping status responses and preserve a newer session-expired state.
- Wait for bounded asynchronous `launchctl bootout` convergence before declaring LaunchAgent removal failed.
- Keep Cargo, npm, lockfile, generated JavaScript, README, and installer version references synchronized.

## Verification

- `npm test`: 28 unit tests plus report, dashboard, and settings Chromium smoke passed.
- `cargo test --workspace --locked`: all workspace, integration, privacy, and documentation tests passed.
- `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and
  `cargo build --release --locked` passed.
- Generated settings JavaScript matched the TypeScript bundle before regeneration.
- A real Codex CLI request against `~/.agent-observability` increased sessions from 49 to 50, LLM spans from
  2,632 to 2,636, tokens from 14,101,407 to 14,138,029, and total spans from 11,178 to 11,183 after a browser
  reload of the private localhost dashboard.

### Independent review record

| Role | Verdict | Evidence |
| --- | --- | --- |
| Code reviewer | APPROVE / CLEAR | Found and rechecked request-ordering, session-expiry, README-version, and LaunchAgent convergence fixes; final review reported zero issues. |
| Verifier | PASS | Re-ran npm and Rust validation and confirmed retry bounds, focus/heartbeat no-reload behavior, stale-session protection, generated-JS parity, and complete version synchronization. |

Both reviewers used the role-separated native agent surface and made no repository changes.

## Non-goals and remaining boundary

- Claude Code and Cursor automatic collection and commercial team operation remain TODO.
- The local dashboard process is bounded and may stop after its idle or hard deadline; `agentobs dashboard`
  starts or reuses the private localhost monitoring surface without affecting stored data.
