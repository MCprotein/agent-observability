# v1.3.2 Release Review

This record captures the patch release that corrects v1.3.1 Package publication. Product runtime
behavior is unchanged; the patch makes npm consume the assembled local tarball, advances
synchronized distribution versions, and preserves both earlier immutable failure tags.

## Final verdict

- **Approved for publication:** exact-head local checks, GitHub CI, a real-package npm dry-run, and
  role-separated review passed.
- **Reviewed source:** `431584f069474c725d01fc5f88d914dc185cd057`.
- **Base:** v1.3.1 merge commit `9065e3289c67ffe48085f805908d3fe472caff04`.
- **Boundary:** no domain, adapter, storage, runtime, report, privacy, or network behavior changed.

## Correction

Tag workflow run `33414142455` built and attested the v1.3.1 artifacts and created a draft Release,
then failed at Package publication. npm interpreted `dist/<package>.tgz` as a GitHub repository
shorthand and attempted to resolve it over SSH. v1.3.2 passes an explicit local path:

```text
npm publish ./dist/mcprotein-agent-observability-1.3.2.tgz
```

The exact argument is regression-locked. A fresh release binary and npm tarball were also assembled
from the reviewed source; `npm publish --dry-run` selected
`@mcprotein/agent-observability@1.3.2` from that local tarball.

## Required verification

- `cargo fmt --all -- --check`: passed.
- `cargo test --workspace --all-targets`: passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `npm test`: 67 tests passed plus desktop, mobile, and 4,096-span browser smoke with no overflow,
  console errors, or external requests.
- `actionlint v1.7.12 .github/workflows/*.yml`: passed.
- Exact-head GitHub `Rust` and `Report UI` checks: passed in run `33414890743`.
- Cargo, root npm, GitHub Package, lockfiles, README commands, architecture/runtime docs, example
  metadata, and release retry fixtures all use `1.3.2`.
- PR head is mergeable and the worktree is clean.

## Independent review evidence

- `code-reviewer` reviewed `431584f069474c725d01fc5f88d914dc185cd057` and returned `PASS` with
  no findings. It confirmed the explicit local path, fail-closed retry behavior, synchronized
  metadata, and accurate v1.3.1 failure history.
- `verifier` reviewed the same exact head and returned `PASS`. It confirmed the one-commit patch,
  immutable tag history, exact-head CI, version parity, and executable dry-run evidence.

## Publication result

PR `#16` merged as `47409ef435807241dd9c6e268289d86201caf978`. Annotated tag `v1.3.2`
peels to that exact merge commit, and tag workflow run `33416302530` completed successfully:

- both native builds and universal2 assembly passed;
- all release archives were attested;
- the GitHub Package step reported `package=published` for version `1.3.2`;
- the Release was promoted from draft to public and non-prerelease;
- all five downloaded assets matched `SHA256SUMS`;
- the downloaded universal executable reported architectures `x86_64 arm64` and version `1.3.2`;
- provenance verification passed for both native archives, the universal archive, and npm tarball.

The public Release is <https://github.com/MCprotein/agent-observability/releases/tag/v1.3.2>.
An independent Package API read was not available because the local GitHub CLI token lacks the
`read:packages` scope; the authenticated publication step and successful workflow are the Package
publication evidence. The roadmap status is now `Released`.
