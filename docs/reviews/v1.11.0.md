# v1.11.0 review checkpoint

Status: development checkpoint, **not release approval**.

## September 7 contention fix and live-scale blocker

Product fix: `1d3e9a3` (adaptive quiet scheduling and integration regressions). Independent
code-reviewer Goodall returned **APPROVE**, and independent architect Godel returned **CLEAR**
for these two code-file changes. Both inspected the final diff before the commit; authoring and
review remained separate. The whole release remains **BLOCKED**, not approved.

- The baseline `dd09e6d` GitHub run
  [34034578088](https://github.com/MCprotein/agent-observability/actions/runs/34034578088)
  passed, including the 96-minute 38-second long job. This does not validate later product changes.
- A private coherent copy of the live database yielded 32,317 typed records in 524 ms without
  snapshot/schema/JSON failure. The live collector had repeated snapshot failures while accepting
  new input. Generation contention is supported by this evidence, but the old coarse health code
  did not disclose the exact live `StoreError`.
- A deterministic slow-snapshot regression reproduced three attempts during a short ingest stream
  on the old scheduler. The fix makes one conflicting attempt, learns a render-sized quiet window,
  then converges after input stops. The window persists across task handoffs and is capped at 30s.
- Fresh full Rust workspace tests, all-target Clippy with warnings denied, formatting and diff
  checks passed. Collector: 87 tests; integration: 59 tests. Full npm tests passed, including
  40 TypeScript unit tests and isolated report/dashboard/settings browser checks.
- The supported installed `connect codex` command restored `config=connected` and
  `notify=external_preserved`. The installed collector is still v1.10.0 and degraded; no candidate
  binary or user-data migration was installed.
- **Additional live-scale failure:** the optimized candidate's complete `report` command against
  another private coherent database copy failed with `report artifact exceeds the 32 MiB contract`
  in 0.95 seconds. Scheduling cannot fix this deterministic capacity failure. Do not delete data,
  silently omit records or increase the cap as an unreviewed workaround. A paged dashboard/report
  design needs a separate scope decision and validation before release.
- Local automatic smoke failed at `lifecycle_setup_failed` before producing performance metrics;
  the cause remains unconfirmed. The local Codex CLI is 0.153.4 rather than the protocol's exact
  0.152.1, but the failure occurred before the explicit version gate, so version drift alone must
  not be asserted as its cause. Fresh exact-head CI/performance validation is still required.
- Chrome extension inspection reused the existing report tab only. It showed connection refused
  after its ephemeral server exited; no new user tab was opened and patched live-browser QA is
  still incomplete.

The earlier checkpoint below is historical evidence, not the latest merge verdict.

Reviewed product revision: `29045fc2f41fc090a832313f5794288285786e4d`.
The independent architect inspected the unchanged product diff before that commit over
`651a94cbe71d`; this document is a subsequent evidence-only addition.

## Independent review

Authoring and review were separated: backend executor Linnaeus, UI executor Faraday,
integration/verification leader, and read-only architect Archimedes.

| Slice | Verdict | Evidence and remaining limit |
| --- | --- | --- |
| Bounded legacy backfill and oversized rows | CLEAR | 128-row/2 MiB pass allowance, persistent incomplete sentinel, later traces progress; blocked traces are not silently deleted |
| Typed maintenance reasons | WATCH | Versioned allowlist flows through collector, integration and TypeScript validation; connect/restart initially returns generic degraded copy until the next detailed status GET |
| Browser opener isolation | CLEAR | Authenticated launch-only endpoint shares Host/Origin/token checks and no-store headers; guarded browser context blocks unmocked open requests; unit-test opener is fail-closed |

These bounded reviews found no product safety blocker. They do not replace the remaining release
performance, CI and live-runtime acceptance gates.

## Fresh verification

- Rust workspace tests passed under the default parallel runner, including local-store 86 passed
  plus one ignored measurement, collector 86 passed, integration 57 passed and local-ui 14 passed.
- Workspace Clippy with warnings denied, rustfmt and diff checks passed.
- Full `npm test` passed: generated-artifact checks, typecheck, 40 unit tests, report, dashboard and
  settings browser smoke. Desktop/mobile/compact settings had no overflow, console error or
  external request; the independent dashboard reloaded after settings exited.
- Settings smoke exercised the actual launch/reuse API without an unmocked platform-opening
  request. Normal `/api/dashboard/open` behavior is preserved; test builds never invoke its OS opener.
- Local performance smoke returned `status=pass` in this worktree. Smoke artifacts are temporary
  and cleaned by the harness; this is not retained normative release evidence.

## Corrected QA claim

Earlier headless settings tests called the real `/api/dashboard/open` twice and opened user Chrome
tabs through macOS `/usr/bin/open`. The prior no-new-tabs claim was incorrect. The offending run
was stopped; tests were resumed only after launch-only routing, a context-level request guard and
fail-closed unit-test opener regressions were in place. Headless mode alone is not OS isolation.

## Documentation and release gates

README/ROADMAP, architecture, configuration, runtime and DESIGN distinguish published v1.10.0
from development v1.11.0. Automatic lifecycle remains off for migrated installations. Warm/Cold
rehydration, destructive whole-trace expiry, raw-detail separation and last-pass health limitations
are documented in `docs/STORAGE_LIFECYCLE.md`.

`anamnesis context diagnose` reports four missing-path warnings in TEAM_ARCHITECTURE and
TEAM_CONTRACTS. Their surrounding text describes planned G1 artifacts, not implemented standalone
features. A missing optional document catalog is informational. No team readiness claim is made.

Pending: exact-head GitHub CI, exact-release-revision long-duration performance evidence and fresh
actual-user-runtime collection QA. No merge, release, installed binary upgrade, production cleanup
or user Codex configuration change was performed at this checkpoint.
