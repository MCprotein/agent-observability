# v1.11.0 review checkpoint

Status: development checkpoint, **not release approval**.

## Current checkpoint — September 9

### Latest reviewed increments

- `7a7eee6` extracts the unchanged report build-ceiling formula into one private
  scalar helper. Code APPROVE and architecture CLEAR cover `59719d00`; both reserve
  deductions, saturating arithmetic, cap and legacy error mapping are unchanged.
  Two snapshot-admission tests, strict collector Clippy and Rust 1.97.0 formatting
  passed independently. The author reported three targeted tests passed; the initial
  missing-helper RED was compilation-only, not a reproduced behavioral defect.
  Report start/finalization, recovery and cleanup policy integration remain pending.
- `bac7bdd` implements the private, dormant CLI collector precommit guard. Code
  APPROVE and architecture CLEAR cover blob `554588bc`; no production caller is
  installed. Nineteen ownership/guard tests and strict all-target CLI Clippy passed
  independently, and Rust 1.97.0 formatting passed. The author also reported the
  full workspace test run passed, with explicit diagnostic/performance ignores.
  Captured policy/revision, complete A/X/U and R, current free space and the existing
  ingest estimate are checked under the supplied freeze. Evidence/probe failures
  remain distinct from numeric denial, and failed postchecks override allowance.
  Whole-guard resource validation and operation-specific integration remain pending.
- `7cc52b5` bounds config reads at 64 KiB plus one detection byte and reuses the
  nonblocking/no-follow descriptor open before checking regular-file type. Code
  APPROVE and architecture CLEAR cover `5733268e`. Oversized acceptance and FIFO
  blocking were reproduced before the fix; the blocked probe was killed and reaped.
  Four focused regressions passed independently. The author reported 45 focused,
  196 runtime unit tests, one process test, strict Clippy, CLI/collector compilation
  and formatting passed. Oversized files are rejected without rewriting them. This
  bounds config input, not the complete ownership observer or collector guard latency.
  The leader also reran all 36 local-UI Rust tests and 11 CLI ownership-composition
  tests successfully against this resource patch before the dormant guard changes.
- `7e6c1d2` separates schema-valid config evidence from operational policy gating.
  Code APPROVE and architecture CLEAR cover `83564421`; the captured policy accessor
  does not enable separated execution. The author reported a pre-fix failing regression,
  41 focused tests, 192 runtime unit tests, one process test, strict Clippy and formatting
  passed. Eleven CLI ownership-composition tests passed independently. Public load/save,
  config service and RuntimeControl continue rejecting unfinished separated operation.
- `e871666` replaces incidental one-second completion guards in three refresh
  behavior tests with the existing finite convergence guard. Code APPROVE covers
  `4960a482`; production bytes and exact attempt/count assertions are unchanged.
  A retained 1.2-second test projection delay failed the old guard. Three exact
  tests, five repetitions of 13 refresh tests, strict collector Clippy and formatting
  passed; an independent coalescing rerun passed in 1.42 seconds. This is a test
  correction, not evidence that historical product contention was repaired.
- `1f5e6f6` shares the existing conservative store-plus-batch estimate without
  changing mode gates or activating a caller. Code APPROVE and architecture CLEAR
  cover `01fe485e`. Eight focused collector-estimate/diagnostic tests and strict
  runtime Clippy passed independently; the author reported 191 runtime unit tests,
  one process test and formatting passed. Enforcement requires root ownership plus
  initialized exclusive accounting freeze through snapshot, decision and completion.
- `9c2dbe1` observes current available filesystem space on the owned freeze's root,
  with pre/post identity checks. Code APPROVE and architecture CLEAR cover `e437ab50`.
  Probe errors retain their I/O kind, including WouldBlock and NotFound, rather than
  becoming lock-busy or missing-barrier diagnoses. The author reported three focused
  tests, 188 runtime unit tests, one process test, strict Clippy and formatting passed.
  This observation is neither reserved disk capacity nor active admission.
- `dff03ec` separates failed-child busy diagnostics from successful-child response
  validation. Code APPROVE and architecture CLEAR cover `85eb390a`; 95 xtask unit
  tests, one privacy test, three focused regressions, strict Clippy and formatting
  passed. A new outcome regression failed before implementation. Accepted outcomes,
  retries and deadlines are unchanged; fixed diagnostic codes expose no raw output.
- `f3c4843` retains finite concurrent-connect substages instead of erasing every
  inner error. Code APPROVE and architecture CLEAR cover `3e5c2ccb`. The 94 xtask
  unit tests and one privacy test passed; the lint-only let-else rewrite then passed
  both focused regressions and strict all-target dependency Clippy. Operations,
  deadlines, successful-outcome requirements and retry behavior are unchanged.
  The next CI failure narrowed to outcome validation, not a proven product cause.
- `1cb4e28` adds an explicit, optional collector precommit guard under the retained
  owned freeze. Code APPROVE and architecture CLEAR cover collector `f155fdaf`
  and port `4051fad0`. Four new regressions passed independently for both OTLP
  and notify, including denial-state preservation, callback suppression, unchanged
  legacy operation and committed-unverified handling. The author reported 167
  collector tests passed/four ignored with the foreground timing test passing
  separately, plus strict Clippy and formatting. The default CLI still uses ordinary
  `serve`; a concrete guard must prove bounded, read-only, non-reentrant behavior
  before activation. This is not complete P3 or separated-policy activation.
- `1008153` adds a full-reservation-total numeric entrypoint without inventing
  active/stale liveness. Code APPROVE and architecture CLEAR cover `eb97092e`.
  The existing split API keeps checked addition and its original error precedence
  through one shared calculation. Fourteen focused tests, 185 runtime unit tests,
  one process test, strict all-target dependency Clippy and CI-version formatting
  passed. This is an inactive arithmetic API, not an operational write permit.
- `35cf297` adds the CLI all-owner observation after existing runtime/store
  preparation. Code APPROVE and architecture CLEAR cover CLI `a1f19414`, composer
  `28e4ecf9`, CLI process tests `7843faae`, and isolated runtime-ownership process
  tests `3fc042d1`. Fresh CLI checks passed (56 unit, 28 process), the two new
  isolated process tests passed, and strict all-target dependency Clippy passed.
  The workspace suite passed with the foreground timing test excluded; that exact
  timing test then passed in isolation, followed by workspace strict Clippy.
  This observes A/X/U, full R and config revision without activating separated
  admission. The command remains migration-capable before the observation;
  unknown entries do not replace legacy admission. No installed-runtime QA is implied.
- `9d286d4` adds noncreating, nonblocking owned-freeze observation. Code APPROVE and
  architecture CLEAR cover `d5df5eb7`; 17 focused tests passed independently, with 183
  runtime unit tests, one integration test and strict Clippy reported by the author.
  Owned/borrowed guards share classification and identity checks; root and accounting
  are each acquired once, with failure releasing already-acquired authority.
- `3acc6d6` strengthens the directory-replacement regression with a callback-stage
  assertion and sanitized error category. Code APPROVE and architecture CLEAR cover
  `395e645b`; 40 exact qualified repetitions, 214 store tests (one ignored), and strict
  Clippy passed locally. The earlier CI failure remains unreproduced and its underlying
  category is unknown. The accepted error variants and product behavior are unchanged.
- `8fc7104` extends foreground root waiting to manual report store preparation and
  publication. Code APPROVE and architecture CLEAR cover CLI `c4ee75a1`, process
  tests `df6def66` and runtime `567a8076`. Fresh CLI tests passed (45 unit plus 26
  process), runtime tests passed (181 unit plus one process), and strict all-target
  dependency Clippy passed. The actual-process pre-store contention regression failed
  before the fix and passed afterward; later publication composition is covered by
  shared-writer and existing acknowledgement tests, not a forced publication-race test.
  Accounting is still attempted once. Root-owning collector paths must continue to
  request render guards nonblockingly to avoid a root/render wait cycle.
- `2488598` adds read-only current-environment ownership capture through integration's
  existing Codex config/home resolution. Code APPROVE and architecture CLEAR cover
  `b09ebd9e`; 84 integration tests and strict all-target dependency Clippy passed.
  Resolver-injected fixtures preserve external FIFO config/plist paths without reading
  them. The wrappers do not activate accounting or expose configuration write capability.
- `6c96574` rejects exact published-snapshot rollback journals before both read-only
  SQLite openings and rechecks after use. Code APPROVE and architecture CLEAR cover
  `e01dc731`; all four new regressions passed independently, the full store suite
  reported 214 passed/one ignored, and strict Clippy passed. Journals are preserved;
  the caller's all-writer freeze remains necessary.
- `a54ee8d` synchronizes all 1,024 status fixture files and the directory before the
  unchanged 250ms foreground timer. Code APPROVE and architecture CLEAR cover
  `b9fb4476`; the isolated test passed, remaining collector tests passed (163 with four
  existing ignored), and strict Clippy passed. This corrects fixture durability;
  the exact syscall responsible for the prior coarse status-publication delay is unknown.
- `539717c` changes foreground integration operations to wait for the validated root
  mutation lock, then attempt accounting exclusion once. Code APPROVE and architecture
  CLEAR cover `3391041c`; 82 integration tests, ten focused repetitions and strict
  all-target Clippy passed. The existing root wait has no internal timeout. Operations
  are not retried, and this does not prove all earlier CI lifecycle failures are repaired.
- `627fb78` nests optional report-view ownership inside the read-only authority
  observation without exposing a write-capable store. Code APPROVE covers
  `cd296720` and `7ae21632`; 210 store tests passed with one existing ignored test.
  Published-snapshot journal checks are the separately reviewed `6c96574` increment above.
- PR CI run `34301237765`, associated with head `83a0820` and tested merge revision
  `ef27678020b77db166ea4006d7c1daed9ddcb6ec`: Linux Rust and Report UI passed;
  macOS Rust tests passed, but automatic smoke failed at the lifecycle reconnect stage
  preflight (before performance measurements). The Linux Clippy failure in prior run
  `34300724726` was repaired by compiling the LaunchAgent owner only on macOS.
  PR #41 remains Draft and unmerged. Private-copy, long performance and
  actual Chrome acceptance remain separate P5 gates.
- `83e6a02` and `d3b5824` narrow recovery-snapshot and reconnect diagnostics without
  changing operations, retries, deadlines or gates. Independent code APPROVE covers
  `9b6b38d1` and `606338ca`; fresh 92 xtask unit tests, one privacy test and strict
  all-target/no-deps Clippy passed. CI outcomes are recorded below. These commits
  do not claim to fix the lifecycle failure.
- `48eb349` adds optional read-only store ownership preflight. Exact store absence
  is revalidated; existing journals are deferred before SQLite access. Independent
  code APPROVE covers `b661df0f`; 206 local-store tests passed with one existing ignored
  test and strict Clippy passed. Main separately reran ten optional-path tests.
  The facade does not recover journals, activate policy, or expose a write-capable store.
- `2b9833a` adds exact static HTML ownership under an exclusive all-writer freeze.
  Independent code APPROVE covers `9d3fe923` and `d7d7cd70`; 31 tests, strict Clippy
  and x86_64 macOS compilation passed. Linux Rust CI passed in `34303127425`.
  No operational admission or installed-runtime acceptance is implied.

### PR CI history for these increments

These are PR merge-checkout runs, not exact-head release evidence. None establishes
successful release performance acceptance; the failed step is listed separately for each run.

| Run | PR head | Tested merge revision | Result |
| --- | --- | --- | --- |
| `34300724726` | `d70690e` | `dfde2d0ef5a8128d8c6fcca71c7f9f9f98506249` | Linux Clippy failed; macOS recovery snapshot failed |
| `34301237765` | `83a0820` | `ef27678020b77db166ea4006d7c1daed9ddcb6ec` | Linux and UI passed; macOS reconnect failed |
| `34302760104` | `d3b5824` | `f7f0ff2b63debf4abd9ec940d62213d6b8829211` | UI passed; Linux isolated private-capture timing failed (467.95ms total, 456.38ms status publication, 250ms limit); macOS concurrent connect failed |
| `34303127425` | `2b9833a` | `62542359cfde355e65d6c3052ee10b419c855a20` | Linux and UI passed; macOS setup command failed |
| `34304275332` | `539717c` | `56af784c33d226ffec2d31c4beb0b7ca6cd34cb1` | Linux and UI passed; macOS recovery snapshot busy; narrower busy subtype not recorded |
| `34305276735` | `a54ee8d` | `a889e8d4a392217f71dd59f197bdb61ee2fea23d` | Linux and UI passed; macOS store suite 213 passed/one failed/one ignored; directory-replacement preflight regression failed before automatic smoke |
| `34306326713` | `3acc6d6` | `c1c422cd06c5490361efb8594bd71d7e15b3bfd4` | macOS tests, repeated convergence, automatic smoke and Clippy passed; UI passed; Linux stopped at formatting before tests (manual-report closure layout) |
| `34307566949` | `35cf297` | `14132355f25a3f2fa4d879df6af80c2da656cea2` | Linux and UI passed; macOS workspace and repeated convergence passed, automatic smoke failed with `lifecycle_concurrent_connect_failed`, Clippy skipped; exact concurrent-connect subtype was not retained |
| `34309210996` | `f3c4843` | `99a7733554a68d0d29db9bfa9ee312201ddb6a05` | Linux and UI passed; macOS workspace and convergence passed, automatic smoke failed with `lifecycle_concurrent_connect_outcome_failed`, Clippy skipped; outcome combination versus successful-output validation remains unresolved |
| `34310223287` | `dff03ec` | `4d9f3027fb2479dfd1ef07128bdbff00b5ffa7a8` | UI and all macOS checks, including automatic smoke and Clippy, passed; Linux formatting and regular tests passed, second report-refresh repetition failed in burst-coalescing completion (one-second guard, attempts=1, scheduled=true, published pending=false); Linux Clippy and exact-release evidence skipped |
| `34312799713` | `7cc52b5` | `d704274dc5cf914960aff8303d5ed361d08859b6` | Linux, macOS and UI passed, including repeated regressions, paged-dashboard checks and macOS automatic smoke; exact-revision automatic release evidence was skipped. This run predates the dormant CLI guard and does not validate it or establish release acceptance |
| `34311417918` | `e871666` | `1d682560889087968e9d3d8138b37e0e51c6a57b` | Linux, macOS and UI passed, including repeated regressions and macOS automatic smoke; exact-revision automatic release evidence was skipped, so this is not release acceptance |

Changing failure stages are not proof of one shared root cause or a successful repair.
The Linux timing result remains a diagnostic concern despite later passing isolated collector checks;
the threshold and durability requirements are unchanged.

### Earlier reviewed increments
- `c4d45b4` rejects journal replacement, hardlink aliases and FIFO swaps at the private
  file-open boundary. Independent code APPROVE covers `57f391bb`; 197 store tests
  passed with one existing ignored test, and strict all-target Clippy passed. This does
  not prove SQLite journal ownership or permit recovery before admission.
- `48b5a35` exposes a read-only config-snapshot facade and narrow config/TLS/LaunchAgent
  coexistence checks. Independent code APPROVE covers `e35249c8` and `fb5f81f7`;
  81 integration tests and strict Clippy passed on macOS. The Linux foreign-LaunchAgent
  regression also passed in Linux CI `34301237765`. This is not a full-root zero-unknown inventory.

- `18ea52d` adds retained LaunchAgent transaction and empty lifecycle-lock ownership.
  Independent code APPROVE and architecture CLEAR cover `3bd2d2fb` and `81e34789`;
  80 crate tests, repeated focused ownership tests and strict all-target Clippy passed.
  Capture uses explicit root/home without reading external plist contents or probing
  processes. The later narrow coexistence test is in `48b5a35`; full composition remains open.
- `8b96e29` adds the four-scope singleton owner and synthetic collector composition.
  Independent code APPROVE and architecture CLEAR cover `ecf9a81d`, `ab79a3d5` and
  `6cd82ffe`. Runtime 178 plus one process test, the three-generation composition and
  strict runtime/collector Clippy passed. Before owner integration, the regression
  reported eleven extra unknown entries; afterward only its deliberate unknown sentinel
  remained. Lock metadata proves file semantics, not process liveness or cleanup authority.
- `d70690e` fixes the settings browser races: independent review found that a late
  config-save response could render settings after shutdown and a retryable shutdown
  failure could target an invisible dialog. Earlier 88 unit tests and browser smoke did
  not cover those interleavings. Final five-file manifest `25386fd9` received code
  APPROVE and architecture CLEAR. Fresh 91 TypeScript tests, authored-JS/typecheck and
  isolated browser smoke passed, including both failure orderings, late save/rebase,
  three viewport sizes and independent dashboard survival after settings exit.

- `89f4de3` adds bounded private raw/status semantic observations and the synthetic
  composition regression for valid unpaired artifacts. Independent code APPROVE and
  architecture CLEAR cover private owner `a62952a5`, parent `0947c628`, shared status
  validator `3978b8d0` and composition `14316b65`. Twenty ownership tests, the exact
  three-generation composition and all-target collector Clippy passed. No payload cache,
  operational admission activation or private-runtime acceptance is implied.
- `2eb0a0f` fixes manual-ingest root contention and deterministic process readiness.
  Independent code APPROVE and architecture CLEAR cover CLI `ab25155b`, process tests
  `10c7954c`, lock `bc7eb315` and writer `6adf84d8`. CLI 45 + process 25, runtime
  170, targeted concurrency and all-target strict Clippy passed. This fixes the local
  cause of the remote CLI failures; both platform workspace suites passed in `34301237765`.
- `5611cf5` adds noncreating Codex config snapshot ownership bound to the manager's
  exact state directory and external config path, without opening that external file.
  Independent code APPROVE and architecture CLEAR cover `2daba75d` and `06460d76`;
  61 tests and strict all-target Clippy passed. Exact retained ancestor matching is
  included; snapshot payload is not retained or emitted by the evidence. This remains
  separate from full collector admission; later LaunchAgent ownership and narrow
  cross-owner composition are recorded above.

- `f9051ad` adds current-v3 TLS ownership using exact retained settings, ancestors and
  credential descriptors. Independent code APPROVE and architecture CLEAR covered
  `4e3f0ccd` plus composition `30a2840e`; 13 collector ownership tests, one synthetic
  composition and strict package Clippy passed. This is not legacy-generation ownership,
  certificate-chain validation or operational activation.
- `f8d2c12` adds root/accounting participation to integration lifecycle creation,
  config snapshot recovery and LaunchAgent transaction writes, releasing storage guards
  before process starts or health waits. Independent code APPROVE/architecture CLEAR
  covered integration `559525e5` and sanitized UI compatibility `981616d1`; 71 integration
  and 27 UI tests plus strict Clippy passed. Snapshot classification and the manual-CLI
  wait-policy fix landed in later commits above, not in this commit.
- Previous remote head `97ab33f` was **not CI-green**: run `34265490855` failed both
  platform CLI concurrency fixtures (`child.try_wait().is_none()`) and the settings
  browser's expected disconnect-error toast. The exact isolated checkout passed the
  local workspace suite and separately discovered mandatory foreground timing test;
  that local evidence does not override these CI failures. Reviewed repairs are now
  pushed at `d70690e`; those checks passed in later run `34301237765`, whose separate
  macOS lifecycle reconnect preflight still failed.
- CLI failure reproduction subsequently captured `storage accounting barrier is busy`:
  manual ingest incorrectly used the common try-only root writer. Repeated direct runs
  of the same exact97ab binary failed; the earlier local PASS occurred when process
  startup exceeded the fixture's 200 ms observation window, not from a different binary.
  The reviewed fix in `2eb0a0f` separates foreground root-lock waiting from try-only accounting
  exclusion and uses an existing-only lock open for initialized roots. Independent review
  rejected timer-based readiness, so both legacy and initialized waiting paths now notify
  only on actual `WouldBlock` on the validated descriptor before blocking on that same
  descriptor. The CLI process fixtures wait for this fixed content-free marker and own
  bounded completion plus kill/reap/reader cleanup. Fresh runtime 170, CLI 45 + process 25
  and strict CLI/runtime all-target Clippy passed; exact independent re-review approved it.
  The settings browser also exposed a real close-during-integration busy-state bug
  after the stale error mock was corrected; the reviewed product fix is in `d70690e`.
- `3e1c441` preserves manual-ingest, retention and report publication outcomes through
  postchecks; 45 CLI unit and 25 process tests passed locally, with independent code
  APPROVE and architecture CLEAR on blob `db1e08a`. The later CI process-test failures
  above passed after the subsequent repairs in CI `34301237765`; this is not full release acceptance.
- `0b3da20` commits collector writer coordination and descriptor-bound settings/TLS
  publication. Independent collector architecture CLEAR covered `fdd5b2a`, and code
  review covered its non-settings scope; settings code/architecture review covered
  final module `7922feba`. A recreated foreign temporary file after publication
  reproduced erroneous cleanup compensation before the fix; the regression now
  preserves published credentials, the migration journal and the foreign file.
- `28003a3` preserves integration committed/uncertain settings outcomes; `97ab33f`
  reconciles uncertain browser actions through validated status before reenabling
  mutation controls. Independent code APPROVE and architecture CLEAR covered both
  slices, with 63 integration tests, 27 UI tests and 85 TypeScript tests passing locally.
  Status reconciliation may perform core recovery and is not filesystem-read-only.
- `3a563d2` composes the existing three-file collector ownership evidence under the
  synthetic accounting freeze; independent code APPROVE/architecture CLEAR and the
  three-generation regression passed. Current-v3 TLS evidence and integration writer
  participation landed in subsequent commits, not in that commit.
- Exact-head CI `34264124017` at `b409dd8` passed Linux, macOS (including automatic
  collection performance smoke) and Report UI. The isolated exact checkout also passed
  runtime 165 + process-lock 1, static-report 15 and strict package Clippy. This closes
  the observed failed lock-release regression at this head; it does not establish the
  uncaptured process history of the older failed CI run. Long release evidence is still
  not satisfied, and later integration commits are not covered by this run.
- `f75b019` preserves stable mutation-lock identity during configuration saves and
  reports primary, mutation and accounting failures without erasing the original
  outcome. The missing initialized lock and replaced legacy lock regressions failed
  before the fix and passed afterward. Independent code APPROVE and architecture
  CLEAR covered final blob `714768c`. Fresh runtime verification passed 165 unit tests, one process-lock test
  and strict runtime/collector Clippy in the integration worktree.
- `4fc6807` retains static-report parent and temporary descriptors through publication
  and guarded cleanup. Architecture CLEAR and code review with no blocking finding
  covered blob `79efaf1`; the code review's Linux/m68k flag correction was applied in
  final blob `f42a44d`. All 15 package tests, strict Clippy and formatting passed again.
  This is observed-identity validation, not filesystem compare-and-swap against
  uncoordinated same-user mutation. An error can follow rename; no compensating
  deletion is performed after publication, and callers must not infer unchanged output.
- `b409dd8` combines root mutation and accounting participation in one owned writer
  scope. Independent code APPROVE and architecture CLEAR covered blob `9b6d2c4`;
  final `7d62b4a` only applies rustfmt wrapping. Optional absence cannot bypass an
  existing barrier; initialized paths do not recreate missing mutation locks. Guard
  destruction explicitly unlocks this acquisition without releasing independent writers.
  The API is coordination only, not storage admission or operational activation.
- `0dff08c` retains dashboard capability and parent-directory descriptors through
  bounded reads, publication and identity-checked cleanup. Independent code APPROVE
  and architecture CLEAR confirmed UI blob `6b5567e`; an isolated exact-commit checkout
  passed 26 UI tests and strict package Clippy. No browser or installed runtime was changed.
- `dfaf474` keeps the existing 250 ms foreground assertion mandatory in both platform
  jobs but executes it separately from the parallel workspace suite. Exact test discovery
  prevents a renamed or missing test from passing with zero tests. Code APPROVE and
  architecture CLEAR; no performance threshold or release gate was relaxed.
- Exact-head CI `34260112798` at `0dff08c` passed Linux and Report UI, but macOS failed
  `coordinated_drop_under_freeze_preserves_metadata_then_creation_recovers_it` with
  `Busy` immediately after singleton acquisition. A local duplicate-descriptor regression
  reproduced delayed accounting-lock release; a fix is under verification. The actual
  CI process-inheritance history was not captured, so this is not a proven full root-cause
  attribution. The long release-evidence job was skipped; this head is not merge-ready.
- `56f1168` adds retained production singleton ownership and CLI wiring after independent
  code APPROVE/architecture CLEAR. Runtime 161 tests plus one privacy test, CLI 32 unit
  plus 25 process tests and strict package Clippy passed. Construction rejects invalid
  locks before replacing metadata; replacement during publication preserves prior metadata
  and foreign locks, and acquired-owner cleanup validates retained identities.
- Exact-head CI `34258336214` at `56f1168` passed Linux, macOS including automatic smoke,
  and UI. Its long release-evidence job was skipped. This includes the Linux cfg correction
  `f6019a0`, independently code APPROVE/architecture CLEAR; the same patch passed isolated
  xtask 91 tests plus one privacy test and strict Clippy locally.
- `50f5d31` synchronizes the RSS fixture: baseline first, explicit child allocation, then
  two additional observed samples. Independent code APPROVE and architecture CLEAR.
  The production 100 ms sampling-gap gate is unchanged. One earlier parallel xtask run
  hit an unrelated fake-binary version-check timeout; the serial 91+privacy1 run passed.
  That timeout's cause was not established by changing the RSS fixture.
- `7e070d2` freezes initialized accounting around automatic canonical ingestion. A known
  durable commit remains acknowledged with its cursor when a later status/barrier check
  fails; health becomes degraded and report refresh remains requested. Transaction errors
  retain their primary cause. Private-content capture has a separate failure receipt and
  never inherits success from the canonical commit. Code APPROVE and architecture CLEAR
  both confirmed collector blob `0c5be18fd74558398d89ed2dc19db6039fc4fd36`.
  Collector 127 tests plus one synthetic integration passed, four tests remained ignored,
  and strict collector Clippy passed.
- A clean detached `7e070d2` checkout with its own Cargo target passed the full Rust
  workspace suite. Exact-head GitHub run `34257086893` passed both platforms' tests,
  macOS automatic smoke/Clippy and UI; Linux Clippy failed because the new macOS-only
  RSS test helpers were compiled under `cfg(test)` on Linux. The narrow platform-gate
  correction was independently reviewed and committed as `f6019a0`. The long release-evidence job was skipped.
  Earlier `f7ced7b` timing-test failures are not declared fixed solely because a later run passes.

UI production wiring is committed in `0dff08c`. Review found a potentially blocking FIFO
capability read, lost legacy singleton identity, and incomplete capability cleanup reporting.
Follow-up testing also reproduced metadata replacement before failed legacy construction.
The singleton, UI, settings/TLS and integration recovery writer fixes are committed above;
full semantic ownership composition remains in progress. These are not merge approval or
separated-mode activation. The remaining P2–P5 gates below still apply.

### Committed core checkpoint

Commit `09c3cab1c039f173503b3ad7f361e299065a74d4` contains the reviewed bounded
inventory, reservation evidence, initial config evidence and common accounting barrier.
A clean detached checkout passed 128 runtime unit tests, one process-lock integration
test and all-target runtime Clippy with warnings denied. Exact-head CI `34246829463`
passed Linux Rust, macOS Rust with automatic smoke, and Report UI with paged-browser
checks. The long release-evidence job was skipped. PR #41 remains draft and unmerged.

Reviewed report/store/collector owner integrations were subsequently committed as
`9d034e0a1bee5ccf294c6840f2c188bbdf053a28`. Neither checkpoint enables separated
storage admission; installation and ingest coordination were separately reviewed and committed
as `f7ced7b` and `7e070d2` respectively.

Exact-head CI `34252282423` failed. Both Linux and macOS exposed a migration test fixture
that directly invoked migration without creating/holding the production store-open lock;
a clean detached checkout reproduced the same failure (194 store tests passed, one failed,
one ignored). The test-only correction `380954b` preserves all rollback/retry assertions,
passes its targeted test and received independent code APPROVE / architecture CLEAR.
macOS additionally failed the subprocess RSS sampler regression; this is
a separate investigation, not evidence of a product capacity failure or permission to lower
the performance threshold. The earlier `2650aa5` CI `34250468536` passed both platforms and UI.

### Config saves and coordinated singleton

`35c04bf` coordinates config saves with an already initialized accounting barrier.
Code review (Banach) approved; architecture review (Kierkegaard) retained an activation
WATCH: strict separated-mode paths must require an existing lock, not optional legacy
discovery. Config 28 and coherence 10 tests passed, including service-level typed Busy
without conflating it with mutation-lock contention. The separated-mode gate stays closed.

`2650aa5` adds coordinated singleton publication and teardown. Newton's code review
and Popper's architecture review closed the prepublication HIGH after collision preservation,
strict metadata validation, repeated identity checks and exact temporary cleanup. A later
FIFO-open concern was corrected and separately approved by Banach and cleared by Popper.
The final lock SHA-256 is
`3619d297c1c7d86dd4120b05e69cac264119149e4546e4cdf499cb0d044abebf`.
The preceding 144 runtime unit tests and one process test passed; the added isolated FIFO
replacement regression also passed. Full workspace tests passed before this final FIFO
addition; strict workspace Clippy, formatting and diff checks passed afterward.

Collector ownership review found unsupported legacy control-file states and blocking FIFO
replacement opens. Shared pure legacy metadata validators preserve production TLS checks;
nonblocking, no-follow opens reject replacements without waiting. Seven ownership tests and
the production migration regressions passed. Banach approved the correction. These owner
changes are committed in `9d034e0` and do not approve TLS files.

Automatic-report review (Carson/Kierkegaard) requested post-phase permit revalidation after
commit/rollback and cleanup. Required factory revalidation now runs while the permit is retained,
including initialization and discard. Primary errors remain primary if both checks fail.
The independent follow-up returned APPROVE/CLEAR with no remaining findings: 49 report-view
tests, the actual 129-record barrier replacement/recovery regression and cleanup contention
test passed. This does not complete the other writer paths or activate separated mode.

Store-authority review (Arendt/Gibbs) closed missing-lock fallback, blocking report-reader
contention and FIFO-open concerns. Seven ownership tests and both missing-lock/nonblocking
regressions passed; code APPROVE and architecture CLEAR. Current reviewed SHA-256:

- `local-store/src/lib.rs`: `c30bbfedbb8e1644c1f17dd2ca9cb1b99179b82c5fa3c43de3ecec1cd1311bec`
- `local-store/src/storage_ownership.rs`: `275607c8255892e86fbcd60ed31655a66e153503b64848117410cd2915fdb6ca`

Subsequent working-tree UI checks passed all82 TypeScript tests and the small paged-browser
profile: 2000 traces/4450 spans, exact filtered KPIs, no external requests or escaped opener
requests. A later full workspace run exposed an install-follow-up regression in the separate
uncommitted config lane: a legacy read-only runtime directory was rejected by strict optional
barrier discovery. That failed run is retained as regression evidence. After correction,
the full working-tree Rust suite passed: collector 124 plus four ignored, runtime 155 plus
one process test, store 195 plus one ignored, and xtask 91 plus one privacy test. Workspace
strict Clippy and formatting also passed. This working-tree evidence includes uncommitted
install/ingest changes and is not exact-head CI or the outstanding macOS RSS correction.
Fresh static-report and localhost dashboard browser smoke passed desktop/mobile, with no
overflow/console/external request errors; this remains isolated synthetic QA, not installed
user-data acceptance.

Installation follow-up `f7ced7b` received code APPROVE / architecture CLEAR after a
deterministic check-to-open lock removal/replacement regression closed the review HIGH.
A fresh detached checkout with its own Cargo target directory passed the full Rust suite.
The earlier detached run sharing another worktree's target is not used as exact-revision
evidence because concurrent builds can replace the test executables. Exact `f7ced7b` CI
`34254060076` nevertheless failed two timing-sensitive collector tests: Linux report-burst
completion hit its one-second harness timeout; macOS contended private capture measured
262.53825 ms against the unchanged 250 ms bound. These failures remain open; neither the
local pass nor unrelated migration/RSS corrections clear them.

### P2 allocation observations and reservation evidence

The bounded filesystem classifier counts A/X/U and zero-byte unknown entries, rejects unsafe
identities/permissions and observed races, and bounds enumeration before exceeding 4096 entries.
Semantic ownership errors propagate. The raw classifier is crate-private and returns a distinct
observation type, with no promotion to admission-policy input. Reservation evidence retains exact
root/lock/metadata descriptors; only bound v2 staging matches. Full captured R survives staging
growth and owner exit; root, metadata and same-name lock replacement invalidate the evidence.

Independent code review (Socrates) and architecture review (Laplace) finished APPROVE/CLEAR
with zero remaining findings. Initial feedback led to the distinct observation type, removal of
the public raw-function export, explicit captured-R naming, lock-replacement regression and
formatting correction. Reviewed SHA-256 fingerprints:

- `reservation.rs`: `5de36fa0d491f6cfac926f216346f62d08092e4840d7f75de41057d590d412bb`
- `storage_inventory.rs`: `0674873ff344a0a45e72d217a75bada2d29fe57165ce8429df2a400d82a493d1`

Fresh scoped evidence: inventory 8 tests, reservation evidence 4 tests and their integration test
passed; scoped rustfmt, diff check and library Clippy passed. Subsequent common accounting barrier,
config identity, catalog observation and coordinated writer work are separate scopes below.
The all-writer integration and operational separated-mode gate remain incomplete. No merge,
release, installed-runtime change or actual-runtime acceptance is implied by this checkpoint.

### Common accounting barrier and config/control identity

Independent code review (Confucius) and architecture review (Hume) completed APPROVE/CLEAR,
including the subsequent owned mutation-plus-freeze scope. Both lanes returned zero remaining
findings. Review fixes added direct captured-versus-candidate descriptor comparison (rather than
only pathname checks), deterministic replacement/restore regressions, typed mutation errors,
strict private reservation-lock revalidation, durable bootstrap retry and explicit guard lifetimes.
The owned scope releases accounting exclusion before root mutation ownership and releases the
consumed mutation guard on failed acquisition; no artificial or cloned lock authority is used.

Reviewed SHA-256 fingerprints:

- `storage_coherence.rs`: `5d9f4e42c34d55d305e1cdfbdccd9c1839539942dfb826225bf98cc768c387e3`
- `config.rs`: `e9aab11737c7ec930fd8e955f36e288a378aaabf469e1bcbb8e1510c6eb00eff`
- `reservation.rs`: `8cbbf1a0fa088050dae30bce9a2aaa0de611e3a84e8a1fe77ec3245cf7477b27`

Fresh workspace tests and all-target Clippy passed; runtime includes 136 unit tests and one
process-lock integration test. TypeScript checks and all 82 unit tests passed. Headless static
report and localhost dashboard tests passed desktop/mobile, including bounded large rendering.
These are local development checks, not exact-commit CI or installed-runtime Chrome acceptance.
The separate singleton/catalog/report-permit changes and cross-crate composition remain under
independent review. Additional store/collector owner adapters are in progress and are not covered
by these fingerprints or the preceding full-workspace test run.

## Current checkpoint — September 8

### P2 staging/reservation binding (implementation under verification)

The collector binds the newly created empty staging descriptor under its runtime mutation guard
before local-store opens SQLite, then releases mutation for projection. The store retains the
created file and parent descriptors; callback rejection prevents database initialization, and
identity mismatch prevents publication or deletion of a replacement through Drop. The runtime
binding and stale-recovery regressions are integrated. Local validation passed: reservation
26 tests, runtime 99 unit + 1 process-lock test, store 169 tests (one existing ignored), collector
112 tests (four existing ignored), full-workspace tests and all-target Clippy. The worst-case
v2 metadata fits the unchanged 512-byte limit; five process-death binding phases preserve
the full reservation. Independent code review APPROVE (zero findings), architecture WATCH:
runtime and store both know the generated staging path contract; current composition tests
cover their agreement, but future layout changes must update both. Combined verdict is COMMENT,
not merge approval. No classifier,
coherent cross-writer measurement or operational separated policy is claimed complete.

CI `34234798096` for `d05d69f` passed Linux Rust and Report UI. macOS tests passed, but
automatic smoke failed before benchmark collection. The uploaded manifest (PR merge revision
`281eae51b044fd9a79c724631ae895a0e5324a02`) contains `lifecycle_preflight_failed` and
`evidence_validation_failed`, with null performance metrics. The exact lifecycle failure is
not recoverable from that artifact; neither a performance regression nor a binding defect
is established. Content-free stage diagnostics are the next bounded investigation step.
Do not treat this run as a green platform or release gate.

The follow-up adds 16 fixed, allowlisted lifecycle stage codes in xtask only. Existing
command order, assertions, deadlines, cleanup and performance gates remain unchanged;
underlying error strings are discarded at the new boundaries. XTASK 90 unit tests and
one privacy integration test, all-target Clippy, rustfmt and diff checks passed. The
new regression checks every code, private-error suppression, failed/not-verified output
and success passthrough. CI `34236524383` at `5562a76` passed Linux/macOS/UI, including
automatic smoke; the long release-evidence job was skipped. This successful run does not
explain the prior lifecycle failure or establish a root-cause fix.

Independent review found a MEDIUM diagnostic defect: a simultaneous cleanup failure could
mask the primary stage. A regression failed with the generic code before the correction.
The mapper now separates only the exact existing cleanup delimiter and matches the primary
stage exactly; private cleanup payloads are never emitted. All 16 compound cases and
near-miss rejection are covered. XTASK 91 unit + 1 privacy tests and all-target Clippy passed;
independent re-review APPROVE/CLEAR closed the finding. Exact-head CI `34237460527`
at `1cc089c` passed Linux/macOS/UI including automatic smoke; long release evidence was
skipped. These diagnostic results do not complete P2/P3 or resolve the earlier intermittent failure.

The prior diagnostic-only `6d4783d` received independent APPROVE/CLEAR and passed exact-head
CI `34231234741` (Linux, macOS including automatic smoke, Report UI). Its long release-evidence
job was skipped. That result does not cover the later binding implementation or establish a
root-cause fix for the earlier private-capture latency failure.

### Notify functional acceptance and foreground deadline

Documentation-only head `8f5358a` CI `34225886133` failed Linux
`notify_projects_before_io_and_uses_only_authenticated_https`: `Unavailable` instead of `Accepted`.
Reservation tests passed. This outcome does not identify a specific transport stage; the CI run
did not capture the underlying error. Host-scheduling/deadline exhaustion is a hypothesis, not a
measured trace or evidence of a storage-policy regression.

The successful HTTPS case now calls the same private notify implementation with a bounded
five-second test deadline. The public entry point still fixes its absolute deadline at 250ms
before projection; no runtime setting, public API, retry or transport fallback was added.
The new deterministic expired-deadline regression checks the 250ms constant, `Unavailable`,
no TCP connection, and projection rejection precedence. Existing slow-drip absolute-deadline
and production-entry negative/privacy tests remain unchanged. Functional success does not prove
production latency under load; the resource/performance gates remain separate and unchanged.
Targeted notify tests and collector tests passed (111 passed, four existing ignored tests unrun).
Full-workspace all-target Clippy, rustfmt and diff checks passed. Full-workspace tests passed with
five existing ignored tests unrun. Independent code review APPROVE, zero findings; independent
architecture review CLEAR, no remaining findings, for this slice only.

### Private capture latency investigation (not a performance fix)

Exact-head CI `34229492246` at `67d1733` passed Report UI and Rust macOS, including
automatic smoke; Linux Rust failed
`private_turn_detail_foreground_path_is_bounded_at_status_capacity_and_lock_contention`.
The receiver returned success, but max-cardinality private capture took 331.27876ms against
the unchanged 250ms assertion. The notify tests passed. The long release-evidence job was
skipped; this is not a green platform or release gate.

The failing test passes in an isolated local run. That does not invalidate the Linux failure
or establish host contention as its cause. Test-only, fixed-label elapsed-time diagnostics
now distinguish mutation-lock acquisition, detail pruning, admission, detail publication,
status pruning and status publication in captured test output. They contain no paths,
identifiers, content or error payloads and are absent from non-test builds. Existing scans,
permission checks, fsyncs, assertions and deadlines are unchanged. These timings are
diagnostic evidence only, not a root-cause fix or a substitute for the latency gate.

### Reservation failure-path lock lifetime

P2 CI `34220304884` at `7c47a32` failed the Linux
`stale_is_counted_until_guarded_recovery_and_wrong_root_is_rejected` test with
`Reservation(Busy)` during recovery after a rejected stale acquire. The new arithmetic tests
passed; this CI failure must not be treated as a green P2 platform gate.

Inspection found that successful `WriteReservation` owners explicitly unlocked on drop, but
failed acquire and empty/error recovery paths closed a raw locked `File` without an explicit
unlock. [Linux flock semantics](https://man7.org/linux/man-pages/man2/flock.2.html) attach the
lock to an open-file description: a surviving duplicate can retain it after another descriptor
closes. The exact failing CI fork/exec interleaving was not instrumented; that attribution remains
an inference, not a proven trace of the CI process.

A deterministic safe-Rust regression retains a `File::try_clone` during actual stale acquire
failure and attempts recovery through an independently opened descriptor. It failed before the
fix with the same `Busy`, then passed after moving unlock ownership into private `ReservationLock`.
This guard exists immediately after successful locking and remains with successful reservation
owners; failed acquisition never unlocks another owner's lock. Empty/corrupt recovery regressions
also retain a duplicate and verify immediate reacquisition without clearing corrupt/stale metadata.
No retry/sleep or test weakening, lock-file deletion, reservation schema/capacity change, unsafe
code, or installed-runtime mutation was added.

Local runtime 91 unit + 1 process-lock tests, full-workspace tests and full-workspace all-target
Clippy passed. Five existing ignored workspace tests were not run; rustfmt and diff checks passed.
Independent code review: APPROVE, zero findings; independent architecture review: CLEAR,
no remaining finding or validation blocker, for this fix only. Exact-head Linux/macOS CI
`34223400587` passed at `5b96e43` (Linux Rust, macOS Rust including automatic smoke, Report UI).
The long automatic release-evidence job was skipped. P2 classification and P3 operation integration
remain gated and incomplete. The source-grounded [ownership inventory](../STORAGE_OWNERSHIP.md)
records target classifications and remaining proof gaps; it is not an operational classifier.

Ownership documentation checkpoint: independent code review APPROVE and architecture CLEAR,
bounded to the five documentation files. One MEDIUM temp-filename finding was corrected against
the static-report writer and re-reviewed closed. Local Markdown target checks (22) and diff checks
passed; context diagnosis retained four unrelated team-document missing-path warnings and one
optional catalog notice. No product code or installed runtime changed in this documentation slice.

### Storage budget P2 numeric foundation

`crates/local-runtime/src/storage_policy.rs` implements pure T/W/F assessment and typed
numeric rejection reasons. It checks allocated-total and reservation arithmetic, workspace
overrun versus insufficient new allowance, filesystem floor, operation-specific retention,
and unknown files including zero-allocated entries. Missing estimates never fall back to zero.
These inputs are **not authenticated filesystem snapshots or write permits**; no operation
calls this function yet, and config load/save/runtime still refuse separated mode.

The real legacy reservation regression verifies that reducing config leaves the full active
and stale promise charged until guarded release, while owner finalization uses the reduced
budget. It does not prove the future separated-mode finalization integration.

Validation: runtime 89 unit + 1 process-lock tests passed (including 12 new arithmetic tests
and the reservation regression); full-workspace all-targets Clippy, rustfmt and diff checks passed.
P1 exact-head CI `34211219175` passed at `49d2d00`; its long release-evidence job was skipped.
That CI does not cover this later P2 slice. Independent code review: APPROVE, zero findings;
independent architecture review: CLEAR, zero findings, for the numeric foundation only.
The reviewer independently reran all 12 arithmetic tests and the config-shrink regression.
The architecture watchpoint is future misuse of a numeric `Ok` as an authenticated write permit;
the operational gate and explicit non-authorization contract remain required.

Source inspection corrected the manual-import contract: commits are per observation/disposition,
followed by projection rebuild, not one atomic whole-import batch. The P0 integration checklist
also records that report reservation identity does not bind a staging path and rendering drops
the mutation guard. A/X/U ownership classification, coherent measurement, guarded policy wiring,
overrun recovery/state, P3 operation coverage and P4/P5 remain pending. No installed data changed.

### Storage budget P1 configuration

P1 implements strict config v5 and generated TypeScript validation with the required
`storage_budget` contract. New and v1–v4 migrated configurations select legacy mode; existing
collection/retention/private-capture/lifecycle choices and inactive budget fields are preserved.
Reading a legacy file does not rewrite it. Supported saves write v5 with the existing guarded,
revision-checked atomic replacement. V4 schemas/fixtures/generated validators remain unchanged.

This is **not separated-policy activation**: pure contract parsing accepts either mode, while
operational load/save and `RuntimeControl` explicitly reject separated mode until P2/P3 is ready.
CLI `storage-bytes` is never reinterpreted as a retention target. No new mode controls are exposed.
Settings reset preserves the hidden budget fields, not just ordinary saves. The new actual
headless reset/save regression first failed with all three values reset to 1 GiB, then passed
after the reset was limited to visible settings. No installed runtime or user Chrome was changed.

Validation of this slice:

- Selected Rust packages: 183 tests (CLI 31+25, contracts 19+14, runtime 76+1, UI 17).
- Independent reviewer: `cargo clippy --locked --workspace --all-targets -- -D warnings` passed;
  leader selected-package/dependency Clippy also passed.
- TypeScript: 82 unit tests, typecheck, authored-JS guard and generated settings bundle check passed.
- Headless settings: desktop/mobile/compact, hidden-field reset/save preservation and independent
  dashboard reload passed; no console errors or external requests in the reported viewport cases.
- Formatting/diff checks passed; the runtime documentation JSON equals the v5 default fixture.
- Integration validator regeneration changes only AJV symbol numbering, verified after normalization.
- Context diagnosis retains four unrelated team-document path warnings and optional catalog info.

Independent code review: APPROVE; its sole LOW finding (stale v4 architecture text) was corrected
and re-reviewed closed. Independent architecture review: CLEAR for this P1 slice only; this is
not merge approval. It independently checked nine targeted TypeScript tests, typecheck, bundle
equality, diff hygiene and retained V4-validator rejection of V5. Future alias-name cleanup and
permanently pinning the historical validator boundary are non-blocking maintenance suggestions.
P2/P3 accounting and operation integration, P4 policy controls, P5 actual-scale/resource/fault/Chrome
acceptance and exact-head CI remain unverified. PR #41 must remain Draft.

### Storage budget policy planning

Follow-up [P0 decisions](../STORAGE_BUDGET_P0.md) define the config v5 fields, legacy-by-default
transition, T/W/F operational defaults/ranges, conservative admission equations and operation
allowances. Independent architect review returned **APPROVE / CLEAR for config P1 only**.
Classification manifest and report allowance integration remain P2/P3 WATCH items; missing
estimates must defer, never become zero. Seven design arithmetic vectors and eight local file
links passed. These are documentation checks, not Rust or installed-scale acceptance tests.
PR #41 was freshly verified OPEN/DRAFT with no merge at base `0363fcb`.

The user approved roadmap-first planning for separate retention target, operational workspace
and filesystem free-space floor; unsafe/FFI exceptions were rejected. The plan is
[Storage Budget Policy](../STORAGE_BUDGET_POLICY.md), with the local execution outline in
`.omx/plans/storage-budget-policy.md`. This is not an activated config or an admission fix.
An independent architect reviewed the plan and roadmap/status notices against base `ae4674e`
and returned **APPROVE, planning-only**. That earlier checkpoint left P0 defaults, ranges,
classification and estimation as prerequisites, now addressed by the scoped follow-up above.
Legacy semantics and deletion
choices are preserved; W is not a hard quota and overruns are not hidden.
Local plan link/line validation passed 31 checks and `git diff --check` passed. Context diagnosis
reported four out-of-scope team-document path warnings and an optional catalog info message;
none concerned the changed policy documents. No product tests were rerun for this docs-only slice.

### Prior implementation and capacity evidence

A fresh private-backup check with the original 4096-byte page size also refused new ingestion
after two retained views. Authority logical size was 406,126,592 bytes against 247,959,552 bytes
of writable headroom: even an optimistic whole-authority-only proxy would exceed it by
158,167,040 bytes before framing/growth/materialization. This closes that proxy as a recovery
for the measured copy; it is not proof that every bounded-write solution is impossible.
The numeric test-only decomposition is documented in
[Staged Ingest](../STAGED_INGEST.md#original-page-size-admission-check--september-8).
Refusal invariance passed; source/runtime stayed unchanged and the disposable copy was removed.
The updated synthetic rotation and complete collector suite passed (110 passed, four explicit
ignored diagnostics), as did all-target dependency Clippy with warnings denied and fmt/diff checks.
The explicit installed-scale diagnostic failed as expected from the unresolved capacity blocker;
it is not counted as acceptance evidence.

Ordinary CI also passed for `4f8c848bad2478331ea551e6dc028070a0db828c` in
[34194053499](https://github.com/MCprotein/agent-observability/actions/runs/34194053499):
Linux Rust, macOS Rust and Report UI succeeded; long release evidence was skipped.
Final local-only 1024/2048-byte report-page experiments both failed capacity without mutating
authority on refusal. The product constant is restored to 4096, all experimental copies are
removed, and page geometry is closed as an insufficient recovery path. No merge or release.

Ordinary CI for `4f4155fdb8c8ee3f47046dcf1fdb45e485c934eb` passed Linux Rust, macOS Rust
and Report UI in [34193177796](https://github.com/MCprotein/agent-observability/actions/runs/34193177796).
Exact-revision long release evidence was skipped, not passed. A subsequent private page-geometry
diagnostic saved 115,431,424 authority bytes at 1024-byte pages but still failed new ingestion
after retaining two views; [Staged Ingest](../STAGED_INGEST.md#private-page-geometry-experiment--september-8)
records parity, measured admission values and the repeatable test. Ordinary CI success does not
close this steady-state capacity blocker or authorize installed migration/merge/release.
The final diagnostic diff passed the synthetic new-ingest rotation test, the complete collector
suite (110 passed, four explicit ignored diagnostics), all-target Clippy with dependencies and
warnings denied, fmt and diff checks. Its independent code review approved with zero findings.
The final private-copy run still failed capacity and verified unchanged authority/cursor/generation/view
after refusal. All four page-experiment copies were removed afterward; the source was untouched.

CI at `1f101a856b358ba6e6ccf8827e51d6036d29afb5` passed Report UI but failed Linux's
privacy-tree notify acceptance assertion and a macOS repetition's snapshot staging `Busy`
unwrap in [34191966510](https://github.com/MCprotein/agent-observability/actions/runs/34191966510).
The macOS workspace test pass succeeded before that repetition failed.

A deterministic descriptor-duplication regression exposed a real render-lock lifetime defect:
closing the guard's descriptor alone left its lock held while a duplicate remained open.
`ReportRenderGuard::drop` now explicitly unlocks before closing. The regression failed before
the fix and passed afterward; it also verifies that closing the stale duplicate cannot release
the next independent owner's lock. No unsafe fork helper or widened retry policy was added.
The local-store suite passed 165 tests with one existing ignored measurement, and an independent
read-only review approved the guard fix with zero findings. This does not establish descriptor
inheritance as the historical CI trigger.

The privacy fixture now separates whole-tree privacy acceptance from the public callback's
250ms fail-open deadline: it uses the real projector and authenticated transport with a bounded
test deadline and production connection capacity. The public callback and latency tests remain
unchanged. Exact durable observation/record counts and the published snapshot all equal two;
this closes the independent review finding that HTTP 200 alone did not prove delivery.
The final targeted privacy test passed and its independent re-review approved with zero findings.
The collector suite passed 109 tests with three existing ignored diagnostics before those final
count assertions were added. An unrelated local xtask repetition timed out in a shell version
fixture containing an unnecessary two-second sleep; that delay was removed without changing the
production five-second timeout or dedicated timeout tests. Five final xtask suite repetitions
passed 89 tests each. Independent review approved the fixture change and Unix/Windows guard-field
rename. Full dependency Clippy for collector/xtask with warnings denied, fmt and diff checks passed.
Fresh remote verification of these follow-ups is still required.

A separate disposable coherent-backup experiment with Apple SQLite 3.51.0 measured ordinary
compaction: 402,747,392 bytes became 395,587,584 bytes and integrity checking passed. The 7,159,808-byte
reduction is insufficient to solve current capacity admission. Internal unused-page bytes must
not be reported as reclaimable savings. Neither copied database was published; installed state,
budget and data remain unchanged. This is diagnostic evidence, not bundled-SQLite migration or
new-data/three-generation acceptance evidence. Both disposable database copies were removed after
measurement; no private database was added to the repository or uploaded.

### Previous checkpoint

CI at `739d60f3a66a42a1b899b03fec0429880f97dc16` passed Linux Rust, all five collector
regression repetitions and Report UI in
[34188855510](https://github.com/MCprotein/agent-observability/actions/runs/34188855510).
macOS collector also passed 109 tests, but the later xtask snapshot-parity test unwrapped a
single `Retry` result after publication. The exact transient source remains unknown.

The test-only follow-up uses the existing bounded convergence loop. A held real render guard
first proves `Retry`, then the first pause releases it; exact generation and record parity
must converge within the unchanged 10-second deadline. Fatal classification, explicit contention,
query-error and corruption assertions remain intact. The one-shot test failed deterministically
with the held guard before repair. After repair the targeted test passed; five parallel xtask
suite runs passed 89 tests each, and the process privacy test, Clippy, fmt and diff checks passed.
Independent Dewey review approved the xtask test diff with zero findings. macOS CI now repeats
the full xtask unit binary five times, retaining concurrent subprocess-test coverage.
The independent reviewer also approved the workflow/TypeScript follow-up with zero findings;
release-package tests passed 12/12 and typecheck passed.
This does not change production locking, error classification, budgets or release readiness.

Earlier CI: candidate `ac7baee306fabd634d72377fc75f7b7e80cebc87` passed macOS Rust
(including automatic smoke) and Report UI, but Linux collector tests failed in
[34185914859](https://github.com/MCprotein/agent-observability/actions/runs/34185914859).
The failures were the three-second contention-convergence timeout and the retry test's
scheduled flag after its roughly two-second polling loop. No failed-smoke artifact was expected
because macOS smoke passed; the earlier smoke failure's cause remains unresolved.

Independent read-only review confirmed two test defects, not a proven production scheduler
race: adaptive quiet can reach 30 seconds, and the retry fault was removed after 120ms even
though the initial debounce is 200ms. A new positive-failure assertion failed on that old test
before repair, demonstrating it did not necessarily exercise a retry. The corrected test retains
the obstruction until a `Snapshot` failure is observed, requires a later attempt, and checks acknowledged
publication, exact record count, dirty-marker removal and cleared failure/health state.
Both affected tests now use a finite two-quiet-window-plus-five-second completion guard with
content-free diagnostics. A pure duration table independently checks growth, ceiling and
saturation, including a valid 3.6-second quiet interval. This is not a latency-budget change.
Linux CI repeats the related tests five times with normal parallelism; no failures are ignored.
Production scheduler, admission, dependencies and installed configuration are unchanged.
Fresh local serial related tests passed 11/11, and five parallel repetitions passed 55/55.
After tightening the expected failure stage, another five parallel repetitions passed 55/55;
the full collector suite passed 109 tests with three existing ignored diagnostics. All 77
TypeScript unit tests, typecheck, authored-JavaScript guard, collector all-target Clippy with
warnings denied, rustfmt and diff checks passed. The final expected-stage retry test passed
independently. No actual user runtime, Docker, Chrome or installed settings were changed.
Independent reviewer Dewey approved the final collector-test/CI/TypeScript diff with zero
findings and closed both prior HIGH test findings. That scoped approval does not establish
the historical Linux trigger or whole-PR/release readiness; prose updates were leader-checked.
The exact Linux failure trigger still requires remote evidence; these findings do not prove
all production races absent or authorize release.

Previous ordinary CI: candidate `5c72448c75c03e19ee0848ce05afcf1d3ea0f059` passed Linux Rust and
Report UI but failed the macOS automatic smoke in
[34184929029](https://github.com/MCprotein/agent-observability/actions/runs/34184929029).
The log contains `automatic_check_failed`; no artifact was uploaded. Its root cause is not
established and must not be equated with the earlier transient cursor `Busy` assertion.
The bounded CI follow-up uploads only the already-sanitized failed-smoke manifest for seven
days, retaining the failed exit status. No raw logs or runtime directory are uploaded. An early
failure without a manifest warns instead of obscuring the original failed check. Regression
coverage failed before the workflow change, then all 11 release-package tests and TypeScript
typecheck passed. Remote artifact delivery still needs fresh CI verification.
All 76 TypeScript unit tests and the authored-JavaScript guard also passed. A fresh isolated
local automatic smoke passed using pinned Codex `0.152.1`, including real Codex E2E, with
`errors: []` in `automatic-1788840382542126000/manifest.yaml`. It records source `5c72448`
with these workflow/docs/test edits uncommitted and `release_readiness: not_verified`.
This does not reproduce or explain the macOS CI failure and is not normative release evidence.
Independent reviewer Dewey approved the bounded workflow/test change with zero findings;
that review did not cover the prose updates or establish the cause of the CI failure.

Previous ordinary CI: candidate `6b1d2230b4a07a730da66beb35f46f0010b2271a` passed Linux Rust,
macOS Rust and Report UI in [34145197604](https://github.com/MCprotein/agent-observability/actions/runs/34145197604).
Exact-revision automatic release evidence was skipped, not passed. The candidate still has no
installed-runtime recovery or normative long-performance acceptance.

The user approved further design scope for new-dependency evaluation and a dedicated local
ingestion process. [Isolated Ingest](../ISOLATED_INGEST.md) proposes a child plus default-VFS
quota shim, with feasibility before integration. No dependency, unsafe exception, product code,
installed migration or budget change is introduced by this design checkpoint. The existing
hard-memory, rollback and actual-scale gates are not waived. The observations below remain
historical evidence for their exact revisions.

Independent architect Boole cleared the isolated-ingest proposal **for P0 feasibility only**
(reviewed design SHA-256 `75dc33c1bb04a293d82d6afd06f90afd8c05cb533b9e44e7f973db9b7e4304d4`,
plan `20fd4b424aa46a2f844a8b0deb2c135a13e3558e1d2b1fd2b6b22ca22c246246`). The two
non-blocking corrections were applied: transaction quota revalidation does not reinstall the
pre-open VFS boundary, and `commit_batch` is cited at line2302. This is not evidence that P0
has passed or that a compatible dependency or macOS hard-memory mechanism has been found.

Subsequent dependency review rejected `sqlite-plugin 0.11.0` and `sqlite-vfs 0.2.0` for the
required safe default-file delegation boundary; native shim authoring would cross the preserved
Rust/unsafe policy. The design now records **P0 source-preflight NO-GO for this candidate**, not
a failed executable test or proof that every alternative is impossible. No P1 integration follows.
Independent reviewer Dewey approved the final design/investigation record with zero findings
(design SHA-256 `a9d6ed37818c6cafed4e20ab7f6f987f1fb7317c14b4c3ea556acce67d17ef65`). This verdict
does not authorize product activation or a policy exception. Document links and diff checks passed;
context diagnosis retains the four existing planned-Team path warnings and catalog information.

### Existing-schema pruning prerequisite

The scalar disposition cutoff preserves the latest-100,000-rowid contract without the retained
`NOT IN` set. Four targeted tests pass, including old/new semantic parity, production-size
rollback/idempotence, pinned emitted-opcode regression (red before the change), and a fixed
100,001-row VM-work comparison with the complete schema/indexes: 1,400,040 → 200,044 steps. This is not wall-clock/RSS or journal
quota evidence. Schema, migration boundaries, raw/privacy projection and admission are unchanged.

Fresh local-store verification passed 164 tests with one existing ignored test; collector passed
108 with three existing ignored tests. Both packages' all-target Clippy, workspace formatting and
diff checks passed. A preceding full-store run failed the existing consumed-summary-cursor test
with `Busy` instead of `SnapshotExpired`; no cause was established or expectation weakened. The
unchanged test passed an isolated run, 20 isolated repetitions and the subsequent full parallel
store run. Preserve this intermittent failure as an unresolved validation risk, not a proven fix.
No new GitHub CI, normative long run, actual-scale ingest acceptance or installed recovery is
claimed for this prerequisite.

Independent code reviewer Dewey approved the initial SQL/test scope with zero findings (library
blob `7112dc3ceb02e91a949499d2b9adc9523847a759`, test blob
`98be4cdcc8b3ba620ae4360126070aba158a5560`). Architect Boole cleared the pruning cleanup to commit
independently but blocked write-reserve reduction: logical row counts do not yet bound B-tree/index,
overflow, freelist/pointer-map, error recovery or peak memory. Existing conservative admission is
a grounded fail-safe, not a masking fallback; no fallback, UI, dependency or unrelated cleanup was
introduced. The regression-first cleanup pass is complete only for this statement.
The leader subsequently corrected the test fixture to create all `SCHEMA_OBJECTS`, not only the
disposition table, so emitted-program and VM evidence includes the production code index. The
four targeted tests passed again; the numeric comparison above is from that corrected fixture.
The corrected fixture passed a fresh full-store run (164 passed, one ignored) and both-package
Clippy/format/diff checks. Dewey approved the final test blob
`a827e5fc9388b4dfa085adf7a9141828c5a17b5a` with zero findings; the library blob is unchanged.

Candidate `4d4261c62a2c912c4cec26e9d87185c79c91f17e` passed
[CI 34135886327](https://github.com/MCprotein/agent-observability/actions/runs/34135886327):
Linux Rust, macOS Rust including automatic smoke, and Report UI. Linux also passed the
physical sparse-file assertion. This is not normative long-run or installed-runtime evidence.

- v7 migration and capacity regressions are implemented; the private-copy measurements below
  remain scoped to their inputs, not an installed migration.
- `d240027` adds the independently reviewed completed-response Chromium cancellation checks
  described in [Paged Browser QA](../PAGED_BROWSER_QA.md). Other transport failures remain strict.
- Actual Chrome extension QA on an isolated demo found a stuck loading header and missing
  token/cost details. `4d4261c` fixes both; Helmholtz and Dewey approved the exact five-file slice.
- The correction passed 75 TypeScript tests, typecheck/build/generated-artifact checks, five
  static-report tests, desktop/mobile checks and a 4,450-span paged smoke in 14.840s. Existing-tab
  Chrome recheck confirmed snapshot state/time and reported usage/cost reasons. Synthetic demo
  acceptance does not establish actual local collection health.
- Long workflow `34135080615` targeted `d240027` and was cancelled before its normative job
  after the Chrome defects were found. It supplies no long-run evidence. The workflow validates
  evidence against the exact source revision: settle final candidate changes before dispatching.
  This is a provenance gate, not a claim that every UI or prose edit changes runtime performance.

Remaining gates: final-head checks, normative long evidence, actual installed collection/recovery
acceptance and integrated release review. PR #41 remains draft; stable is v1.10.0. Historical
checkpoints below do not override this current status.

### Setup and admission diagnosis — September 8 follow-up

[Workflow 34137212497](https://github.com/MCprotein/agent-observability/actions/runs/34137212497)
tested `0144690`: Linux Rust, macOS Rust and Report UI passed, but the normative job failed
about three seconds after invoking the automatic command. Its retained manifest reports
`lifecycle_setup_failed`; sustained measurements never started. That generic historical code
cannot establish which setup sub-step failed.

A deterministic regression reproduced a separate setup defect: an active report publication
guard caused dashboard preparation to return a fatal recovery error. Preparation now retries
only typed mutation/publication contention, releasing its mutation guard between attempts,
with a two-second retry deadline. Invalid catalogs remain fatal and unchanged. Ownership-rebase
diagnostics distinguish command, preparation-busy, recovery, config and collector stages without
publishing raw errors. These changes do not retroactively prove the old workflow's cause.

The setup correction passed isolated automatic smoke with pinned Codex `0.152.1` using both debug
and optimized product binaries. After admission-diagnostic integration, 55 CLI tests and Clippy
passed; xtask diagnostics passed 88 unit tests and one privacy integration test. The optimized
binary also passed a fresh isolated smoke after shared-admission integration. These were
uncommitted development checks, not exact-head release
evidence or installed-runtime acceptance.

Read-only installed-runtime diagnosis established a separate conservative write-admission refusal:
root allocation plus another store-sized write allowance and the maximum batch exceeded the
effective writable partition, although root-plus-batch alone fit. A root-only `runtime-check`
therefore does not establish collector write admission. Runtime status, manual ingest and collector
ingest now share the conservative assessment. Status includes numeric reservation, headroom and
deficit fields; regressions preserve active/stale reservations and reject unsafe accounting.
Removing the journal allowance without
transaction/rollback bounds, raising the configured budget, or deleting observations is not a fix.
The installed v1.10 runtime has not been migrated or replaced by this correction.

### Approved staged-ingest follow-up

The user approved expanding into the storage-engine admission contract. The
[staged-ingest design](../STAGED_INGEST.md) preserves the configured budget and ordered transaction.
Architecture review cleared disabled proof instrumentation but blocked reduced-admission runtime
enablement: input size does not bound archived-trace restoration, topology fan-out or ledger pruning,
and the RSS p95 target is not a hard peak-memory limit. Current plus retired paged views must remain
charged during a three-generation ingest/publication acceptance run.

The 1 GiB default originated as a planning budget in `9284f3d` and was implemented in v0.13.0
at `6e6b9f1`; it was not derived from a documented average-user storage measurement. README and
configuration guidance now explain that distinction, the configurable range and temporary-space
admission. This clarification does not change configuration or benchmark thresholds.

[CI 34142238567](https://github.com/MCprotein/agent-observability/actions/runs/34142238567)
on `d8732af` passed macOS Rust and Report UI but failed a Linux xtask assertion: the real-store
catalog-corruption probe returned `Retry` where the test demanded immediate `Fatal(Catalog)`.
The log does not identify which retry-class transient occurred. The follow-up separates deterministic
fatal-classification/no-extra-sleep coverage from real-filesystem convergence, which may observe
typed transient state before it reaches catalog parsing. Production error classification is unchanged;
the integration test still requires the catalog-fatal outcome within the existing deadline.

That xtask correction passed on Linux in the next workflow, `34144137571` at `4a9a748`.
The same workflow instead exposed a collector external-commit watcher test's two-second timeout.
The historical timeout alone does not identify the slow stage. A new regression deterministically
reproduces the harness mismatch by starting with a legal learned quiet window of 2.5 seconds:
the old two-second deadline fails before publication can begin. The convergence harness now
allows the existing 30-second quiet ceiling plus five seconds, retains the same completion
assertions and reports only bounded numeric/enum diagnostics on failure. It does not change
product scheduling or claim a new publication-latency SLO. Both watcher scenarios and all 108
collector unit tests passed locally (three existing tests remain explicitly ignored); related
all-target Clippy passed. Subsequent Linux CI on `6b1d223` passed as recorded above.

The disabled storage proof adds a private before-commit observation seam without changing ordered
transaction writes. Two regressions passed locally: mixed observation/disposition/correlation
rollback and retry, and restoration of four archived rows from one new input followed by rollback
and retry. Their observed precommit journal sizes are not a measured peak or a safe runtime quota.
Original invalid-payload and crash/reopen coverage is preserved. The hard write-set/memory bound
and interleaved actual-scale ingest/publication acceptance remain open.
The first whole-store run exposed a reused temporary fixture label in the new observer test;
the new fixtures now have distinct labels. A fresh whole-store repeat passed 160 tests with one
existing test explicitly ignored, and local-store all-target Clippy passed. This closes the
test-fixture collision, not the runtime admission gate.

## Earlier follow-up: approved v7 acknowledgement migration

The user approved expanding the scope to the fixed-width acknowledgement table and v6→v7
migration. Implementation is in progress; this supersedes the approval-pending decision below,
but does not constitute migration or release approval for an installed runtime.

- The development schema isolates the eight-byte acknowledgement from variable-size metadata.
  Migration validates legacy state, retires the legacy key transactionally and preserves
  generation, visibility epoch and canonical observations. See
  [Acknowledgement Storage](../ACKNOWLEDGEMENT_STORAGE.md) for the enforced write contract.
- Regression checks cover legal page sizes, full-u64 values, unsafe layouts/modes, interrupted
  migration/acknowledgement processes and unchanged authority after admission refusal.
  An authentic synthetic fixture produced at baseline commit `9cd3a826843789e479f95c5856e033054b50b60c`
  also passed stale-reservation catalog cleanup, failure/retry preservation and subsequent migration.
  The actual baseline accessor accepted v6 and rejected current v7. Jason independently approved
  closure of both compatibility-evidence findings at collector blob
  `b30ef6451dcffa2ef450febc2f689c3c65313444`; this is not aggregate architecture approval.
- Full Rust workspace tests and all-target Clippy with warnings denied passed after integrating
  the fixture regression. The nine focused ACK tests and TypeScript tests/typecheck/build also
  passed. Later admission changes require fresh checks.
- The current small headless browser check passed with 2,000 traces and 4,450 spans: exact KPI
  filtering, pagination, deletion-cursor handling, zero external requests and zero escaped opener
  calls. This is isolated synthetic-data evidence, not actual Chrome acceptance.
- A later final-candidate repeat failed the unchanged strict browser assertion on one span
  request (`unexpected_abort`, response started, signal not aborted, no fetch rejection, stage
  `trace_traversal`). API summary/traversal passed first. The earlier successful run therefore
  does not establish final browser readiness; this intermittent failure is a separate merge blocker.
- The first coherent v4 copy was refused by a full-database temporary-space allowance. The
  replacement staged transaction measures its journal and potential authority-page writes before
  commit, without raising the configured budget. The initial 32-page growth ceiling was too small
  for the existing 100,000-row disposition index. Review also caught the 512-byte-page limitation
  of a fixed 1,024-page replacement. The final ceiling derives from admitted bytes and page size,
  bounded by 64 MiB of growth, and actual growth is charged in final admission.
- A subsequent private copy with 47,049 records and 45,088,402 canonical bytes passed v7 migration
  and three report rotations in 92.49 seconds under the unchanged 1 GiB budget. Maximum sampled
  tree allocation was 823,083,008 bytes; canonical identity and retirement checks passed. The
  ten-millisecond sampler is not a proof of all instantaneous peaks. The source was not migrated
  and the private diagnostic copies were cleaned up.
- Separate backup/migration-only measurement passed in 3.42 test seconds (4.93 process seconds),
  with maximum process RSS 101,154,816 bytes and zero swaps. This is measured evidence for that
  input, not a universal memory ceiling. It excludes report building, unlike rotation measurements.
- APFS on this host materialized seek-created gaps in the synthetic freelist fixture. The local
  test therefore proves freelist reuse, not physical sparse-file behavior. Linux CI retains a
  physical-hole allocation assertion; that platform-specific result is still pending.

Merge remains blocked on final independent integrated review and fresh final-head/platform/CI
validation. No installed-runtime migration or release was performed here.

## Earlier follow-up: crash recovery, abort ownership and schema boundary

- Independent review found that the original terminal cleanup discarded a busy-lock result.
  The follow-up retains scheduler ownership for four bounded cleanup-only attempts, reports
  exhausted contention/fatal/join failures through existing static degraded stages, and preserves
  the promise for later wake/startup recovery. Typed busy errors survive mutation, catalog and
  reservation-lock boundaries. Held-lock-then-release coverage for all three locks and bounded
  retry-exhaustion tests passed without a report rebuild. Full collector verification passed
  (105 tests, two opt-in diagnostics ignored), along with dependency Clippy, formatting and diff
  checks. Helmholtz independently approved H2 closure at collector blob
  `53b205b17a39348bc29fe11561d6e9de0085efe1`; this is not authority-v7 or release approval.
- Runtime repair `cff8c5d`, integrated by `8c4f3d4`, separates the stable lifetime lock from
  atomically published private metadata. Read-only lookup no longer uses a creating directory
  helper. Worker evidence: 61 unit tests plus process-lock integration, seven process-exit phases,
  scoped Clippy and formatting passed. This is not simulated hardware-power-loss evidence.
- Collector follow-up adds a cleanup-only attempt after terminal retry exhaustion. Its regression
  injects a post-reservation failure through all four attempts and verifies current-view preservation,
  pending authority, cleared stale ownership and restored unrelated write admission without restart.
- A reproduced UI defect suppressed unowned `AbortError` failures. Task-local controller ownership
  now controls suppression and cleanup. Paired owned/unowned cancellation regressions passed;
  Jason independently approved the UI/test/generated-bundle slice, not the release.
- Strict browser checks were not relaxed. After the UI correction and content-free diagnostic
  enrichment, small and large headless profiles passed in 23.17s and 103.28s. The large profile
  covered 16,000 traces, 32,450 spans, 161 API trace pages, 80 UI trace pages and three selected-span
  pages, exact KPI scope, deletion cursor invalidation, zero external requests and zero escaped
  opener calls. A successful repeat does not establish the initiating cause of the previous
  intermittent Chromium request aborts. Existing-tab Chrome acceptance remains pending.
- The remaining acknowledgement bound is a material schema decision. A bounded read-only `dbstat`
  check returned an internal metadata page followed by a leaf, both 4096 bytes. The proposed
  single-leaf guard is therefore incompatible with actual data. Same-length text replacement is
  also incomplete at decimal digit boundaries. Architect Boole recommends a dedicated singleton
  fixed-width 8-byte acknowledgement value with a versioned v6→v7 migration, crash recovery,
  legacy-key retirement and fail-closed downgrade semantics. This scope is **not yet approved**;
  no authority schema or actual database was changed.

Merge remains blocked on that decision and final integrated review/CI/performance/Chrome evidence.

## September 7 shared-reservation and v2-index integration

The user approved shared runtime write reservations after the original three-generation diagnostic
failed inside the default 1 GiB managed budget. This is not a full-device disk exhaustion claim.

- Runtime reservation code is integrated from `4c9a8aa` and `2df7b19`; active and interrupted
  reservations count against ordinary admission and migration headroom. Collector integration
  uses short mutation guards, reloads settings at final publication and retains failed-finalization
  reservations for guarded recovery. CLI `runtime-check` uses the same admission path.
- Store change `79bfb89`, integrated as `696503d`, adds narrow v2 dimension indexes and preserves
  validated v1 query kernels. Independent code-reviewer Jason approved exactly those five store
  files with no findings; this does not approve the integrated runtime or release.
- A private coherent copy containing 47,049 records and 45,088,402 canonical bytes completed
  three rotations in 92.92 seconds. Each new sidecar was 132,370,432 logical bytes; the maximum
  sampled managed-tree allocation was 825,536,512 bytes. Canonical identity/count and normal
  retirement checks passed, and the private diagnostic copy was cleaned. Ten-millisecond
  sampling is measurement evidence, not a proof of every instantaneous write bound.
- Full Rust workspace tests passed after integration (collector 102 passed, two opt-in diagnostics
  ignored; runtime 55 unit plus process-lock coverage; store 142 passed, one ignored). Later
  corrections require fresh exact-head checks.
- Independent architecture review remains **BLOCK** on proving the authority acknowledgement
  journal allowance. Its separate acknowledgement-mismatch finding is corrected with a passing
  regression; the reservation now survives mismatch for guarded recovery.
- The fresh large headless browser run reached exact API summary/traversal, but failed its strict
  network assertion on two unexpected trace-request aborts. It is **not** a browser pass, and the
  assertion must not be weakened without causal evidence. Existing-tab Chrome QA is still pending.

PR #41 remains draft/open. No candidate installation, original-data migration, remote push, merge,
release or new long-performance run was performed for this checkpoint. Remaining gates are the
acknowledgement write bound, browser abort diagnosis, independent integrated re-review and fresh
exact-head local/CI/performance evidence.

## September 7 paged-dashboard scope and prerequisites

The user approved expanding v1.11 to a bounded paged dashboard while preserving source data and
the self-contained HTML export. [Paged Dashboard](../PAGED_DASHBOARD.md) records the accepted
immutable sidecar, generation/visibility separation, bounded work and honest KPI semantics.

- Commit `e9cd0fd` adds the scope documents, single-span Rust projection and shared validation,
  plus content-free automatic setup diagnostic categories. It is pushed to draft PR #41.
- Code-reviewer Turing approved the initial five-code-file delta with no findings. Architect
  Feynman cleared the scoped checkpoint after requiring physical retirement of old-epoch
  sidecars and closure of query/builder handles under the publication guard. This is not approval
  of subsequent store/query implementation or of the release.
- A late QA-only smoke-evidence preservation addition to `xtask` was included in `e9cd0fd`;
  separate exact-commit re-review found two medium issues: smoke readiness was overstated and
  retained evidence could precede cleanup failure. The corrected delta computes final evidence
  after cleanup, keeps smoke readiness `not_verified`, and passed 81 unit tests plus one privacy
  test. Turing's fresh scoped re-review approved the corrected file hash
  `9dbbd69486c9f900b6dedda50e2f9296c66a09c2` with no findings; rustfmt and diff checks passed.
- Application 24 tests, contracts 19 tests, then full Rust workspace tests passed at the foundation
  checkpoint. Scoped Clippy with warnings denied, formatting and diff checks passed.
- A new isolated smoke passed setup and failed the explicit Codex version gate with PATH version
  0.153.4. Repeating with a private installation of the CI-pinned 0.152.1 passed the smoke profile
  including real Codex E2E. Temporary installation/runtime resources were removed; global Codex,
  actual user runtime/config and Chrome were unchanged. These diagnostics used an older prebuilt
  candidate binary, so they are not exact-head release or long-performance evidence.
- Bounded store scanning, shared repository-context finalization and staging index construction
  are subsequent work in progress. No query endpoint or paged browser surface is certified yet.

Whole PR/release remains **BLOCKED** on implementation, capacity/concurrency/privacy/deletion
verification, existing-tab Chrome QA and fresh final-head release gates.

## September 7 contention fix and live-scale blocker

Product fix: `1d3e9a3` (adaptive quiet scheduling and integration regressions). Independent
code-reviewer Goodall returned **APPROVE**, and independent architect Godel returned **CLEAR**
for these two code-file changes. Both inspected the final diff before the commit; authoring and
review remained separate. The whole release remains **BLOCKED**, not approved.

- The baseline `dd09e6d` GitHub run
  [34034578088](https://github.com/MCprotein/agent-observability/actions/runs/34034578088)
  passed, including the 96-minute 38-second long job. This does not validate later product changes.
- A private coherent copy of the live database yielded 32,317 typed records in 524 ms without
  snapshot/schema/JSON failure. The live collector had repeated snapshot failures while accepting
  new input. Generation contention is supported by this evidence, but the old coarse health code
  did not disclose the exact live `StoreError`.
- A deterministic slow-snapshot regression reproduced three attempts during a short ingest stream
  on the old scheduler. The fix makes one conflicting attempt, learns a render-sized quiet window,
  then converges after input stops. The window persists across task handoffs and is capped at 30s.
- Fresh full Rust workspace tests, all-target Clippy with warnings denied, formatting and diff
  checks passed. Collector: 87 tests; integration: 59 tests. Full npm tests passed, including
  40 TypeScript unit tests and isolated report/dashboard/settings browser checks.
- The supported installed `connect codex` command restored `config=connected` and
  `notify=external_preserved`. The installed collector is still v1.10.0 and degraded; no candidate
  binary or user-data migration was installed.
- **Additional live-scale failure:** the optimized candidate's complete `report` command against
  another private coherent database copy failed with `report artifact exceeds the 32 MiB contract`
  in 0.95 seconds. Scheduling cannot fix this deterministic capacity failure. Do not delete data,
  silently omit records or increase the cap as an unreviewed workaround. A paged dashboard/report
  design needs a separate scope decision and validation before release.
- Local automatic smoke failed at `lifecycle_setup_failed` before producing performance metrics;
  the cause remains unconfirmed. The local Codex CLI is 0.153.4 rather than the protocol's exact
  0.152.1, but the failure occurred before the explicit version gate, so version drift alone must
  not be asserted as its cause. Fresh exact-head CI/performance validation is still required.
- Chrome extension inspection reused the existing report tab only. It showed connection refused
  after its ephemeral server exited; no new user tab was opened and patched live-browser QA is
  still incomplete.

The earlier checkpoint below is historical evidence, not the latest merge verdict.

Reviewed product revision: `29045fc2f41fc090a832313f5794288285786e4d`.
The independent architect inspected the unchanged product diff before that commit over
`651a94cbe71d`; this document is a subsequent evidence-only addition.

## Independent review

Authoring and review were separated: backend executor Linnaeus, UI executor Faraday,
integration/verification leader, and read-only architect Archimedes.

| Slice | Verdict | Evidence and remaining limit |
| --- | --- | --- |
| Bounded legacy backfill and oversized rows | CLEAR | 128-row/2 MiB pass allowance, persistent incomplete sentinel, later traces progress; blocked traces are not silently deleted |
| Typed maintenance reasons | WATCH | Versioned allowlist flows through collector, integration and TypeScript validation; connect/restart initially returns generic degraded copy until the next detailed status GET |
| Browser opener isolation | CLEAR | Authenticated launch-only endpoint shares Host/Origin/token checks and no-store headers; guarded browser context blocks unmocked open requests; unit-test opener is fail-closed |

These bounded reviews found no product safety blocker. They do not replace the remaining release
performance, CI and live-runtime acceptance gates.

## Fresh verification

- Rust workspace tests passed under the default parallel runner, including local-store 86 passed
  plus one ignored measurement, collector 86 passed, integration 57 passed and local-ui 14 passed.
- Workspace Clippy with warnings denied, rustfmt and diff checks passed.
- Full `npm test` passed: generated-artifact checks, typecheck, 40 unit tests, report, dashboard and
  settings browser smoke. Desktop/mobile/compact settings had no overflow, console error or
  external request; the independent dashboard reloaded after settings exited.
- Settings smoke exercised the actual launch/reuse API without an unmocked platform-opening
  request. Normal `/api/dashboard/open` behavior is preserved; test builds never invoke its OS opener.
- Local performance smoke returned `status=pass` in this worktree. Smoke artifacts are temporary
  and cleaned by the harness; this is not retained normative release evidence.

## Corrected QA claim

Earlier headless settings tests called the real `/api/dashboard/open` twice and opened user Chrome
tabs through macOS `/usr/bin/open`. The prior no-new-tabs claim was incorrect. The offending run
was stopped; tests were resumed only after launch-only routing, a context-level request guard and
fail-closed unit-test opener regressions were in place. Headless mode alone is not OS isolation.

## Documentation and release gates

README/ROADMAP, architecture, configuration, runtime and DESIGN distinguish published v1.10.0
from development v1.11.0. Automatic lifecycle remains off for migrated installations. Warm/Cold
rehydration, destructive whole-trace expiry, raw-detail separation and last-pass health limitations
are documented in `docs/STORAGE_LIFECYCLE.md`.

`anamnesis context diagnose` reports four missing-path warnings in TEAM_ARCHITECTURE and
TEAM_CONTRACTS. Their surrounding text describes planned G1 artifacts, not implemented standalone
features. A missing optional document catalog is informational. No team readiness claim is made.

Pending: exact-head GitHub CI, exact-release-revision long-duration performance evidence and fresh
actual-user-runtime collection QA. No merge, release, installed binary upgrade, production cleanup
or user Codex configuration change was performed at this checkpoint.
