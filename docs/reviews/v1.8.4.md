# v1.8.4 Release Review

Status: **Released**. This record covers SQLite operational-error fidelity and deterministic report concurrency
coverage. The reviewed source passed the exact-source gate, was merged and tagged, and the public artifacts were
verified after publication. A separate one-machine smoke checked the installed local path without changing the
official Codex compatibility pin.

## Scope

- Preserve SQLite busy and other operational query failures as `StoreError::Sqlite` instead of reporting false
  schema or integrity corruption.
- Keep missing or malformed required metadata and actual schema-contract violations as
  `StoreError::SchemaMismatch`.
- Replace scheduler- and wall-clock-dependent sustained-write assertions with channel-controlled batch-write
  coverage that proves report callbacks execute after the bounded read transaction closes.
- Batch generation-fence fixture setup while preserving the 128/129/256/257 boundary cases.
- Remove a test-only collector startup race with a bounded health-readiness probe before the single authenticated
  notify request; production deadlines and retry behavior remain unchanged.

## Safety boundary

This release does not change the five-second production busy timeout, rollback journal mode, transaction
boundaries, report generation fence, retry policy, durable schema, privacy projection, or stored data. The error
mapping changes only required text queries whose SQLite failures were previously collapsed into a corruption
message.

## Verification

- A real exclusive SQLite lock with a zero-timeout test reader must produce
  `StoreError::Sqlite(DatabaseBusy)`; a missing required metadata row must remain `SchemaMismatch`.
- The callback/write and generation-fence tests must pass repeatedly without sleep-based overlap or elapsed-time
  thresholds.
- Workspace tests, clippy, formatting, TypeScript checks and browser smoke must pass on the exact PR head.
- Independent architecture, code and test reviews must approve the exact source.
- Exact-source automatic Codex evidence must pass before tagging.

The release candidate source was `c928e298c9bc399752cd0360edcc4ae20443e519`. GitHub Actions run
`33780398919` passed Linux Rust, macOS Rust/performance/clippy, and Report UI jobs. The exact-source automatic
release evidence run `33780806420` also passed. Independent architecture, code, and test reviewers approved that
exact source after the migration-query mappings and test-only collector readiness fence were corrected.

### Independent review record

| Role | Exact-source verdict | Finding and resolution |
| --- | --- | --- |
| Architecture | CLEAR | Confirmed that timeout, journal mode, schema, privacy projection and production retry boundaries were unchanged |
| Code | APPROVE | Required migration text queries initially collapsed operational failures; all required-schema call sites were moved to the shared typed mapping before approval |
| Test | APPROVE | An initial yield-based collector-start synchronization remained scheduler-dependent; it was replaced with a bounded authenticated health-readiness fence before approval |

The stable implementation and regression evidence is the exact source above, PR `#35`, CI run `33780398919`,
automatic evidence run `33780806420`, and the induced-lock, migration, report-concurrency and collector privacy
tests committed in that source. The role verdicts describe the independent review gate; GitHub PR review objects
were not used as the review transport.

## Non-goals

- No WAL migration, longer busy timeout, silent retry expansion or weakened corruption checks.
- No Codex compatibility-version change.
- Claude Code/Cursor automatic collection and commercial team operation remain TODO.

## Publication evidence

- PR `#35` merged as `869791e97c0168eed1bbb2961e5ec3cbd1c3233a`, and annotated tag `v1.8.4`
  resolves to that merge commit.
- Release workflow `33790634168` passed on attempt 2. Attempt 1 had one content-free x64
  `automatic_check_failed` after the exact-evidence and arm64 gates had passed; the isolated failed-job rerun
  passed without a source change, then published the GitHub Package and public Release.
- All five distributable assets matched `SHA256SUMS` and passed GitHub artifact attestation verification:
  arm64, x64 and universal2 archives, the npm package, and `install.sh`.
- A public `install.sh` upgrade installed both `agentobs` and `agent-observability` as version `1.8.4`. Hashes of
  the existing Codex config, config-ownership snapshot, LaunchAgent-ownership snapshot, and LaunchAgent plist
  were unchanged by installation.
- Two consecutive `agentobs setup --no-open` runs reported `config=connected`,
  `notify=external_preserved`, and `collector=ready`. Exact comparisons against the pre-upgrade backup confirmed
  that the Codex config, both ownership snapshots, and LaunchAgent plist remained unchanged after both runs.
- One real Codex `0.153.0` execution completed successfully and increased the private store from 7,576 to 7,582
  observations and from 7,575 to 7,581 records. This is a one-machine post-publication smoke, not capability or
  release verification and not a promotion of the official Codex `0.152.1` compatibility pin. A fresh mode-`0600`
  dashboard was generated from the updated store; private cumulative activity totals are intentionally omitted.
- Chrome Computer Use could not navigate directly to the local `file://` report because its browser URL policy
  blocks that scheme. No URL-policy workaround was used; visual/browser regression remains covered by the exact
  source Report UI job, while local publication QA verified the generated report artifact and its aggregate data.
