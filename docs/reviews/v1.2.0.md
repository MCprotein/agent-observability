# v1.2.0 Release Review

This record captures the completed review and verification for the standalone retention release.
The release adds explicit bounded retention and private archive export to the local-only product
path without introducing a daemon, identity, endpoint, outbox, or network dependency.

## Final verdict

- **Released:** every required verification gate completed successfully.
- **Reviewed source:** `fe8da2e9b2bb9cbc088c4df7f551ca423ad9d097`.
- **Normative evidence:** `docs/evidence/local/performance/1788152070592764000/manifest.yaml`
  reports `status: pass` for five baseline and five enabled runs.
- **Review result:** independent code, architecture, and documentation review found no blocking
  issue. Exact-head GitHub Rust and Report UI checks passed before the evidence-only release update.
- **Boundary:** the release remains macOS standalone private canonical handoff import. Native agent
  hooks, receiver, producer, team collection, hosted UI, and wider platform support remain Future
  TODO or unsupported as documented below.

## Delivered boundary

- `local_runtime.v2` adds strict retention limits for age, records per pass, and archive bytes while
  loading v1 configuration with explicit defaults.
- SQLite `local_state.v4` migrates v1, v2, and v3 authority and adds typed indexed retention
  selection, bounded expired-span guards, incremental vacuum mode, and applied-plan receipts.
- Retention plans select whole traces whose observations are all older than the UTC-day cutoff.
  Newer and unresolved traces remain pinned; stale incomplete traces are eligible so adapters
  without a terminal session signal can still reclaim old data.
- Apply writes and syncs a private no-overwrite JSONL archive before one immediate deletion
  transaction, then incrementally reclaims a bounded page count and rebuilds the current-record
  projection.
- The archive contract is closed and runtime-neutral. It contains sanitized durable records plus
  bounded header/footer metadata and excludes source cursors, event IDs, payload hashes, raw opaque
  IDs, prompts, outputs, and source payloads.

## Review findings resolved

- A trace that exceeds the remaining pass bounds stops the deterministic plan without being split;
  apply rejects every truncated plan so no selected prefix is partially removed. The operator must
  raise one of the bounded archive limits and create a new plan before applying.
- Candidate sizing and archive publication stream records from indexed SQLite authority. Selection
  retains only bounded trace IDs and span hashes, without cumulative archive-record or event vectors.
- Expiry guard insertion and source-input deletion use one set-wise operation instead of per-event
  or per-trace scans; only query paths that reduce repeated work keep persistent indexes.
- Old cursor replay fails closed. The newest 100,000 expired span hashes preserve semantic replay
  suppression and changed-state conflict behavior; completed receipt history is capped at 1,024,
  with receipt completion and pruning committed atomically.
- The applied-plan receipt binds recovery to the canonical archive path, plan metadata, archive
  length, and original whole-file SHA-256. A same-size internally valid archive substitution is
  rejected.
- Receipt recovery runs before selection. Pre-commit archive publication and post-commit compaction
  failures have separate retry contracts:
  a pre-commit retry uses a new path, while a post-commit retry resumes the original receipt and
  archive.
- The CLI apply path enters receipt recovery directly without a preliminary plan scan. A pending
  receipt blocks every different pass; completed receipts, dispositions, and expired-span guards
  have explicit tested caps.
- Legacy schema fixtures remove every v4-only object and restore non-incremental auto-vacuum.
  Migration bounds the legacy disposition ledger before its full rewrite, requires explicit
  workspace admitted against configured and physical free space, and preserves the old schema
  version after a failed row conversion for repair and retry.
- Archive retries hold one stable exclusive lock per archive parent and remove only strict private
  crash-left temporary names under a bounded total directory scan while the store transaction excludes
  another retention writer.
- Managed-root path canonicalization rejects archive placement inside the runtime even when the
  runtime argument itself is relative.
- The release fixture drains the fixed 64-slot ingress through one CPU execution token and one
  normalization actor. Each batch is capped at 32 records and 512 KiB, bounding the durable handoff
  to 576 KiB and total ingress-to-writer payload to about 4.6 MiB. Baseline and enabled
  supported-rate passes use the same 3 ms driver inter-arrival schedule; a separate enabled-only
  unpaced saturation pass verifies rejection, latency, bounded memory, and durable reconciliation.
  A durability barrier commits every supported accepted event before saturation begins.
  Supported CPU includes the accepted durable commit tail through barrier completion.
  Peak CPU sampling uses the profile-declared interval rather than a hidden high-frequency loop.
- CPU phase samples use process CPU-time deltas over matching wall-time intervals rather than
  process-lifetime `%CPU`. Disk evidence accounts for the complete release durable root across all
  retained run directories. Host evidence is sanitized but factual; placeholder values fail closed.
- macOS network evidence uses one PTY-backed process-lifetime monitor per run, not one `nettop`
  process per resource sample. Only a cycle closed by the following header is accepted; each resource
  sample requires completed evidence no older than three seconds, and the run retains the maximum
  across closed sockets. The worker remains alive until a complete cycle that started after drain
  completion is observed. Linux uses point-in-time socket-descriptor scans at each resource sample and
  after drain. Startup, parse, stale, missing-sample, and unexpected-exit errors fail immediately.
- The worker creates the drain marker only after the drain command and removes it only after drain
  completion plus the parent-confirmed final resource sample. Protocol, process, sampler-result, and
  local process-query waits are bounded, and completed sampler/output-reader threads are joined.
- A failed run stops the profile immediately. Worker and sampler termination and join waits are
  bounded, resources whose completion is confirmed are joined, durable payloads are removed, and the
  release failure manifest is retained for diagnosis.

## Required verification

- `cargo fmt --all -- --check`: passed.
- `cargo test --workspace --no-fail-fast`: passed, including migration, retention, crash recovery,
  archive path safety, schema, privacy, and performance-protocol fixtures.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `npm test`: 59 tests passed, followed by desktop, mobile, and 4,096-span `file://` browser smoke
  with no overflow, console errors, or external requests.
- `cargo run -q -p agent-observability-cli -- contracts`: passed with team ingest disabled.
- `cargo run -q -p xtask -- perf local --profile smoke --check`: passed.
- Strict archive-schema compilation, migration fixtures, retention plan/apply subprocess smoke,
  privacy/legal reference scan, disabled product-network scan, browser bundle URL scan, manifest
  YAML parsing, and `git diff --check`: passed.

## Independent review evidence

- `code-reviewer` reviewed source revision
  `fe8da2e9b2bb9cbc088c4df7f551ca423ad9d097` plus the passing normative manifest and returned
  `CLEAR` with no blocking findings.
- `architect` reviewed source revision `fe8da2e9b2bb9cbc088c4df7f551ca423ad9d097` and returned
  `CLEAR` with no blocking findings.
- `writer` reviewed the release documentation and passing manifest for consistency and found no
  prohibited named external observability vendor or repository reference. Its requested release
  closure edits are incorporated in this record, README, and roadmap.

## Performance evidence

The normative five-hour local-runtime protocol was rerun for v1.2.0 because retention changes the
local configuration and durable-store implementation. Its sanitized passing release manifest is
retained at `docs/evidence/local/performance/1788152070592764000/manifest.yaml`. It measures the
foreground runtime ingress boundary and does not claim native agent hook, receiver, producer,
report-render, or archive-apply latency.

### Passing normative evidence

- The manifest is bound to source revision
  `fe8da2e9b2bb9cbc088c4df7f551ca423ad9d097` and protocol revision
  `v1.2.0-supported-rate-saturation-continuous-network`.
- All five baseline and five enabled runs completed with no manifest errors. Enabled durable
  reconciliation passed, and every run reported zero network bytes with continuous process-lifetime
  monitoring on macOS.
- Foreground hook latency was 69 microseconds at p95 and 21,088 microseconds at p99, below the
  respective 20 ms and 50 ms limits.
- Supported-rate CPU peaked at 37.23% against the 100% limit. The enabled-minus-baseline active CPU
  delta averaged 0.020% and peaked at 0.431% for any minute, below the 2% and 5% limits.
- Enabled RSS p95 was 8,688 KiB against the 96 MiB limit. Total allocated disk was 563,834,880 bytes
  against the 1 GiB limit.

### Retained normative failures

- `1788079743936450000` is preserved as an explicit failed legacy attempt from source revision
  `e93aa19b72ac1385d71491b40d198dc648d53b0d`; it predates mandatory protocol revision metadata and
  failed because process RSS became unavailable.
- `1788095477499512000` records the unpaced-burst CPU failure from source revision
  `2dae36e2b052739c9fb962936db8535a891a9137`. It remains `status: failed`; the supported-rate and
  saturation protocol replaced that ambiguous workload.
- `1788117956222440000` records the required-resource-metric failure from source revision
  `1aa4bd598ba08b1560abe70fbf73a0d886a28c8b`. Four per-sample macOS network probes were missing;
  validation failed closed even though the old aggregate omitted those missing values and displayed
  zero. The replacement uses one bounded continuous monitor and retains its process-lifetime maximum.
  The retained failure evidence and recovery were reviewed and committed before the passing
  normative attempt; these entries remain historical diagnostic evidence.

## Deferred scope

- Expired records no longer contribute to subsequent reports. Correction, retraction,
  reinstatement, archive restore, and deterministic aggregate rebuild across retention remain
  unsupported until a privacy-safe contribution journal and versioned checkpoints exist.
- Automatic scheduling and archive lifecycle management are not installed; the operator chooses
  when to plan/apply and owns exported files outside the runtime root.
- Native receiver/producer integration, wider platform support, and team/commercial infrastructure
  remain Future TODO promotion work.
