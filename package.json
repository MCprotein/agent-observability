{
  "name": "agent-observability",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Local-first observability primitives for coding agents.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
